{
  "name": "esp32-gw-multi-sensor-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Browser regression tests for the ESP32 gateway dashboard",
  "scripts": {
    "test:browser": "playwright test",
    "test:browser:firefox": "playwright test --project=firefox",
    "test:browser:debug": "playwright test --debug",
    "test:browser:headed": "playwright test --headed",
    "mock-server": "node tests/mock-server/server.js",
    "fixtures": "node tests/fixtures/generate-fixtures.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "html-minifier-terser": "^7.2.0"
  }
}
