# secrets-example-v7.3.4.2.yaml
# Version-aligned copy of the unchanged example secrets file.
# Unchanged content carried forward into the v7.3.4.2 versioned bundle.
# Copy relevant keys into your real secrets.yaml
wifi_ssid: "YOUR_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"

# Management credentials for destructive actions in the dashboard.
# These are required by POST /api/reboot and POST /api/delete-data.
gateway_mgmt_username: "ESPadmin"
gateway_mgmt_password: "StrongPasswordHere"
