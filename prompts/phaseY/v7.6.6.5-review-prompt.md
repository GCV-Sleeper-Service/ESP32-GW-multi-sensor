# v7.6.6.5 — Review Prompt

_Review checklist for the v7.6.6.5 PR (NVS persistence device test gate)_

---

## Instructions

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Read thoughtfully `prompts/handoff/phaseY/session-handoff-v7.6.6.5.md` to understand the current stage and deliveries.

### Step-Specific Review Checklist

- [ ] **No source code changes.** `git diff` shows ZERO changes to files in `firmware/core/`, `scripts/`, `tests/`, or any YAML. Only `Docs/changelog.md` and version-related files changed.
- [ ] **Boot log evidence present.** PR description includes boot log excerpt showing NVS restore messages (slot count, segment count, no crash).
- [ ] **Storage-stats evidence present.** PR includes `/api/storage-stats` output with valid partition sizing.
- [ ] **History endpoint evidence present.** PR includes `/api/v2/history/{device}/{metric}` output with populated data.
- [ ] **Reboot persistence evidence present.** PR includes pre-reboot and post-reboot `/api/storage-stats` comparison showing segment counts did not decrease.
- [ ] **Correct curl usage.** All POST commands in the PR evidence used `-d 'a=1'` (not `-H "Content-Type: application/json"`, not `-d ''`, not bare `-X POST`).
- [ ] **Correct board/YAML.** Firmware was flashed using committed `firmware/esp32-c3-multi-sensor.yaml` (not a generated YAML).
- [ ] **Assembly identity holds.** `bash scripts/assemble-sensor-history.sh --check` passes.

### Acceptance Criteria Verification (cross-reference §6)

- [ ] Boot restore successful (evidence in PR)
- [ ] History retention survives reboot (evidence in PR)
- [ ] `/api/storage-stats` valid (evidence in PR)
- [ ] `/api/v2/history/{device}/{metric}` returns data (evidence in PR)
- [ ] All Playwright tests pass (all 4 fixture sets)
- [ ] Preflight passes
- [ ] Assembly `--check` passes

### Device Test Evidence Completeness

- [ ] Boot log captured
- [ ] Test 1 — storage-stats output recorded
- [ ] Test 2 — history endpoint output recorded
- [ ] Test 3 — live data output recorded
- [ ] Test 4 — status output recorded
- [ ] Test 5 — reboot persistence pre/post comparison recorded
- [ ] Test 6 — hourly persist (documented as observed or skipped with reason)

### Documentation Completeness

- [ ] Changelog entry for v7.6.6.5
- [ ] Version bumped
- [ ] Session log produced
- [ ] Instruction Compliance Output table in PR

---

## Post-Merge Deliverables

Produce BEFORE closing the session:
- `prompts/phaseY/v7.6.6.5-PR<NN>-consolidated-audit-and-lessons.md`
- Review and update: `prompts/handoff/phaseY/session-handoff-v7.6.6.6.md` and `prompts/phaseY/v7.6.6.6-implementation-instructions-for-coding-agent.md`

Tag: `git tag -a v7.6.6.5 -m "Phase Y: NVS persistence device test gate PASSED" && git push origin v7.6.6.5`

---

_End of v7.6.6.5 review prompt._
