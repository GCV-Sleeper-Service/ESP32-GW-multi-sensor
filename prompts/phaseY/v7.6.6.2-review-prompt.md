# v7.6.6.2 — Review Prompt

_Review checklist for the v7.6.6.2 PR (fragment-level preflight checks)_

---

## Instructions

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Read thoughtfully `prompts/handoff/phaseY/session-handoff-v7.6.6.2.md` to understand the current stage and deliveries.

### Step-Specific Review Checklist

- [ ] **Two new preflight checks added.** `firmware_core_assembly_check` and `firmware_core_fragment_line_sum` both exist as functions in `scripts/preflight.sh`.
- [ ] **Both checks pass.** Run `bash scripts/preflight.sh` and verify both show PASS.
- [ ] **Assembly check calls `--check` mode.** The preflight function calls `bash scripts/assemble-sensor-history.sh --check`, not a manual diff or naive SHA-256.
- [ ] **Line sum is dynamic.** The preflight function compares fragment total to committed file total dynamically (reads both at runtime), not against a hardcoded 4,325.
- [ ] **No assembly `--check` at pipeline tail.** Verify `provision.sh` `run_full_pipeline()` does NOT contain `assemble-sensor-history.sh --check` after the final pipeline step. Assembly check is in preflight only.
- [ ] **Check ordering correct.** In `preflight.sh` main sequence: `firmware_core_fragments_exist` → `firmware_core_assembly_check` → `firmware_core_fragment_line_sum`.
- [ ] **Error messages helpful.** If either check fails, the error message tells the operator what to run to diagnose (e.g., "run: bash scripts/assemble-sensor-history.sh --check").
- [ ] **No changes to assembly script.** `scripts/assemble-sensor-history.sh` unmodified.
- [ ] **No changes to fragment files.** No files in `firmware/core/` modified.
- [ ] **No changes to provision.sh.** `scripts/provision.sh` unmodified.

### Acceptance Criteria Verification (cross-reference §6)

- [ ] Both new preflight checks pass
- [ ] All existing preflight checks pass (no regressions)
- [ ] Assembly `--check` passes after full pipeline run
- [ ] `esphome config` validates
- [ ] All Playwright tests pass (all 4 fixture sets)

### Pipeline Verification

- [ ] Full pipeline runs successfully
- [ ] `render_sensor_config.py --check` passes
- [ ] Assembly `--check` passes (via preflight)

### Documentation Completeness

- [ ] Changelog entry for v7.6.6.2
- [ ] Version bumped
- [ ] Session log produced
- [ ] Instruction Compliance Output table in PR

---

## Post-Merge Deliverables

Produce BEFORE closing the session:
- `prompts/phaseY/v7.6.6.2-PR<NN>-consolidated-audit-and-lessons.md`
- Review and update if necessary: `prompts/handoff/phaseY/session-handoff-v7.6.6.3.md` and `prompts/phaseY/v7.6.6.3-implementation-instructions-for-coding-agent.md`

Tag: `git tag -a v7.6.6.2 -m "Phase Y: Fragment-level preflight checks" && git push origin v7.6.6.2`

---

_End of v7.6.6.2 review prompt._
