# v7.6.6.0 — Review Prompt

_Review checklist for the v7.6.6.0 PR (provision.sh full pipeline automation)_

---

## Instructions

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Read thoughtfully `prompts/handoff/phaseY/session-handoff-v7.6.6.0.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and ask for a review specifically checking:

### Step-Specific Review Checklist

- [ ] **Pipeline ordering correct.** The `run_full_pipeline()` step array matches Critical Rule 37 exactly: bundle → render → fixtures → render → build → minify → header → check. No steps missing, no steps reordered.
- [ ] **Assembly placeholder present.** Step 0 prints a skip/placeholder message for `assemble-sensor-history.sh`. It does NOT attempt to execute a nonexistent script.
- [ ] **`--dry-run` works.** `bash scripts/provision.sh satellite --dry-run` prints all steps with `[DRY-RUN]` prefix and makes zero filesystem changes.
- [ ] **`status` non-mutating.** `bash scripts/provision.sh status` does NOT call `run_full_pipeline()` and does NOT modify any files.
- [ ] **No `eval` usage.** Pipeline steps execute without `eval`. Direct `$step` execution or explicit command calls only.
- [ ] **Dependency pre-checks.** `require_node()` and `require_npm_deps()` are called at the start of `run_full_pipeline()`, before any pipeline step.
- [ ] **Error handling.** If any pipeline step fails, `run_full_pipeline()` exits with a clear error message identifying which step failed. No silent continuation.
- [ ] **CI-safe warning preserved.** Non-satellite modes still warn about pushing to remote.

### Acceptance Criteria Verification (cross-reference §6)

- [ ] `satellite` mode runs all 8 active steps
- [ ] `aggregator` mode runs full pipeline
- [ ] `wroom` mode runs full pipeline
- [ ] `--dry-run` prints without executing
- [ ] `status` is non-mutating
- [ ] `preflight.sh` passes
- [ ] All Playwright tests pass (all 4 fixture sets)

### Migration Safety Verification

- [ ] No changes to `sensor_history_multi.h`
- [ ] No changes to any firmware file
- [ ] No changes to any test file
- [ ] No changes to any build script other than `provision.sh`
- [ ] `esphome config firmware/esp32-c3-multi-sensor.yaml` validates

### Documentation Completeness

- [ ] Changelog entry present for v7.6.6.0
- [ ] LESSON-OPS entry added to `Docs/lessons/operations.md`
- [ ] Version bumped via `bump-version.sh`

### Instruction Compliance Output Table

- [ ] Present in PR description
- [ ] All prompt requirements mapped to implementation

---

## Post-Merge Deliverables

IMPORTANT: Once PR is merged, please double-check Workflow and Post-PR Closure Deliverables sections and produce what is required.

Post-merge deliverables — produce these BEFORE closing the session:
- `prompts/phaseY/v7.6.6.0-PR<NN>-consolidated-audit-and-lessons.md` using questions from `prompts/phaseY/pr-audit-question-template-phaseY.md` (stable core + pre-step supplement)
- Review `prompts/handoff/phaseY/session-handoff-v7.6.6.1.md` and `prompts/phaseY/v7.6.6.1-implementation-instructions-for-coding-agent.md` — update any stale references if the provision.sh changes affect those assumptions

Only then merge and tag: `git tag -a v7.6.6.0 -m "Phase Y Pre-step: provision.sh full pipeline automation" && git push origin v7.6.6.0`

---

_End of v7.6.6.0 review prompt._
