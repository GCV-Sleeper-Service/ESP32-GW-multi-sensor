# v7.6.6.1 — Review Prompt

_Review checklist for the v7.6.6.1 PR (assembly script + 8 fragments + SHA-256 baseline)_

---

## Instructions

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Read thoughtfully `prompts/handoff/phaseY/session-handoff-v7.6.6.1.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and ask for a review specifically checking:

### Step-Specific Review Checklist

- [ ] **Fragment count.** Exactly 8 files in `firmware/core/`: config.h, data-model.h, nvs-persistence.h, deferred-management.h, ping-adapter.h, aggregator-runtime.h, web-handler.h, registration.h.
- [ ] **Line counts match plan.** Run `wc -l firmware/core/*.h` and verify: 95 + 460 + 614 + 50 + 168 + 891 + 2006 + 41 = 4,325.
- [ ] **SHA-256 identity gate passes.** Run `bash scripts/assemble-sensor-history.sh --check` — must exit 0.
- [ ] **Direct concatenation identity.** `diff dashboard/sensor_history_multi.h <(cat firmware/core/config.h firmware/core/data-model.h firmware/core/nvs-persistence.h firmware/core/deferred-management.h firmware/core/ping-adapter.h firmware/core/aggregator-runtime.h firmware/core/web-handler.h firmware/core/registration.h)` must exit 0 (no differences).
- [ ] **Assembly script `--list` correct.** `bash scripts/assemble-sensor-history.sh --list` prints all 8 fragments with correct line counts.
- [ ] **Assembly script `--check` generator-aware.** Read the `strip_generated()` function — it must strip content between `SENSOR_MANIFEST:HEADER_BEGIN`/`END` and `SENSOR_MANIFEST:ENTITY_BEGIN`/`END` before comparing SHA-256 hashes.
- [ ] **No code changes.** Fragments are pure line-range extractions — no additions, no modifications, no headers, no separators.
- [ ] **Boundary landmarks verified.** Spot-check 3 fragments:
  - `head -1 firmware/core/data-model.h` — should start with `TAG` definition (line 96)
  - `head -1 firmware/core/aggregator-runtime.h` — should start with `#if AGGREGATOR_ENABLED` (line 1388)
  - `head -1 firmware/core/registration.h` — should start with a comment block (line 4285)
- [ ] **Assembly step activated in provision.sh.** The Step 0 placeholder from v7.6.6.0 is replaced with actual `assemble-sensor-history.sh --write` execution.
- [ ] **Preflight fragment check added.** `firmware_core_fragments_exist` check function exists in `preflight.sh` and passes.
- [ ] **YAML unchanged.** `firmware/esp32-c3-multi-sensor.yaml` `includes:` still references only `../dashboard/dashboard.h` and `../dashboard/sensor_history_multi.h` — no fragment files.
- [ ] **`sensor_history_multi.h` unchanged.** The assembled artifact is NOT modified by this step — fragments are copies.
- [ ] **MODULES array order.** The assembly script MODULES array lists fragments in the correct order: config → data-model → nvs-persistence → deferred-management → ping-adapter → aggregator-runtime → web-handler → registration.

### Acceptance Criteria Verification (cross-reference §6)

- [ ] All 8 fragment files exist
- [ ] Line counts match exactly
- [ ] Assembly `--check` passes
- [ ] Preflight passes (including new check)
- [ ] `esphome config` validates
- [ ] All Playwright tests pass (all 4 fixture sets)
- [ ] No behavior change

### Migration Safety Verification (all 12 rules)

1. [ ] Structural only — no behavior changes
2. [ ] All Playwright tests pass (all 4 fixture sets)
3. [ ] All preflight checks pass
4. [ ] `esphome config` validates
5. [ ] Endpoint contracts unchanged
6. [ ] NVS schema unchanged
7. [ ] Step independently revertable
8. [ ] Post-Phase X dashboard/test/build assumptions preserved
9. [ ] Generated artifacts remain valid (`render_sensor_config.py --check`)
10. [ ] Deferred-task patterns survive (visible in assembly order)
11. [ ] Mutex/lock scope survives (single definition in assembly)
12. [ ] `maybe_yield_nvs_scan_()` survives (single definition in `nvs-persistence.h`)

### Documentation Completeness

- [ ] Changelog entry for v7.6.6.1
- [ ] Version bumped via `bump-version.sh`
- [ ] Session log produced
- [ ] Instruction Compliance Output table in PR

---

## Post-Merge Deliverables

IMPORTANT: Once PR is merged, produce these BEFORE closing the session:

- `prompts/phaseY/v7.6.6.1-PR<NN>-consolidated-audit-and-lessons.md` using `prompts/phaseY/pr-audit-question-template-phaseY.md`
- Review `prompts/handoff/phaseY/session-handoff-v7.6.6.2.md` and `prompts/phaseY/v7.6.6.2-implementation-instructions-for-coding-agent.md` — update any stale references if actual line ranges or assembly script behavior differed from plan assumptions

Only then tag: `git tag -a v7.6.6.1 -m "Phase Y: Establish assembly script and 8 fragment baseline" && git push origin v7.6.6.1`

---

_End of v7.6.6.1 review prompt._
