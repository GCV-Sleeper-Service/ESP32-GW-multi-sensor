# Prompt Corrections — v7.6.0.2 and subsequent Phase D prompts
#
# Apply these corrections BEFORE running the coding agent.

## v7.6.0.2 — DELETE /api/aggregator/satellite/{id}

### Correction 1: Critical Rules count
File: prompts/phaseD/v7.6.0.2-implementation-instructions-for-coding-agent.md

FIND (§2 item 5):
  All 42 Critical Rules (especially 1, 2, 4, 5, 26, 27, 28, and new rules 29–35)
REPLACE WITH:
  All 44 Critical Rules (especially 1, 2, 4, 5, 26, 27, 28, 29–35, 38–44)

FIND (§7):
  All 42 Critical Rules apply.
REPLACE WITH:
  All 44 Critical Rules apply.

### Correction 2: Current Status (§3)
FIND:
  - v7.6.0.1 complete and merged (add-satellite endpoint operational)
REPLACE WITH:
  - v7.6.0.1 complete and merged (add-satellite endpoint operational)
  - v7.6.0.1 post-merge fixups applied: BUG-077 (`String`→`std::string`),
    BUG-078 (local component `init_response_()` HTTP status code mapping),
    canHandle() GET routing for proper 405 responses
  - HTTP error responses now return correct status codes (BUG-078 fixed)
  - Critical Rules now total 44 (Rules 43–44 added)

### Correction 3: §7 Critical Rules table — add rows
ADD to the Critical Rules table:

| 43 | After `patch-esphome-httpd-stack.sh`, verify `init_response_()` status switch | BUG-078 |
| 44 | Never use Arduino `String` — always `std::string` in ESP-IDF code | BUG-077 |

---

## v7.6.0.3 through v7.6.0.5 — same corrections

Apply the same Critical Rules count update (42→44) and add Rules 43–44 to the
Critical Rules table in each prompt.

Additionally for v7.6.0.4 (Dashboard UI):
- All dashboard `fetch()` POST calls must use `Content-Type: application/x-www-form-urlencoded`
  with `body: 'a=1'` (Critical Rule 38). This is already in the prompt but worth
  re-emphasizing given BUG-076/078 history.

Additionally for v7.6.0.5 (Playwright tests):
- Mock endpoints must return correct HTTP status codes matching the API contracts.
  BUG-078 is now fixed so firmware returns correct codes; mocks must match.
