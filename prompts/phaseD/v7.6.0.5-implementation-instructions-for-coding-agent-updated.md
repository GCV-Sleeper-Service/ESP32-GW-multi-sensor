# v7.6.0.5 — Playwright Tests + Phase D Closure (Coding Agent Prompt, UPDATED Hybrid)
**Scope summary:** Add stateful mock + test coverage and close Phase D docs/versioning.
**Date:** 2026-03-27
**Prerequisite version:** v7.6.0.4 merged and device-tested.

---

## 1) Required Reading (§3.2)
1. `Docs/phase-d-implementation-plan.md` (v7.6.0.5 closure scope).
2. `Docs/writing-prompts-for-coding-agents-guide.md` (§3.11, §3.12, §9 checks 12/13, §14 lessons).
3. `prompts/prompt-index-and-workflow.md` (Critical Rules 1–35).
4. `tests/mock-server/server.js` (route conventions + helpers).
5. `tests/browser/dashboard.spec.js` (derive next group number from source).
6. `Docs/bugs-and-lessons-learned.md` (LESSON-OPS-081/082/083, 043, 077).
7. Closure docs: architecture plan, phase-d plan, aggregator-setup, changelog.

---

## 2) Current Status (§3.3)
- v7.6.0.4 complete.
- Firmware endpoints + UI are available.
- This step delivers stateful mock/testing + closure docs.

---

## 3) Pre-condition Checks (§3.4)
```bash
FIXTURE_SET=3sensor npx playwright test --project=chromium
FIXTURE_SET=mixed npx playwright test --grep "Mixed" --project=chromium
FIXTURE_SET=system npx playwright test --grep "System" --project=chromium
FIXTURE_SET=aggregator npx playwright test --grep "Aggregator" --project=chromium
bash scripts/preflight.sh
```

---

## 4) Exact Scope (§3.5)
### 4.1 New Playwright group
- Add new numbered test group in `dashboard.spec.js`.
- Number must be derived from current highest group + 1.

### 4.2 Stateful mock routes
Implement in `tests/mock-server/server.js`:
- `POST /api/aggregator/add-satellite`
- `DELETE /api/aggregator/satellite/:id`
- `POST /api/aggregator/test-satellite`

State must persist within test process:
- add → visible later
- delete → removed later
- test endpoint remains read-only

### 4.3 Minimum scenario matrix
1. add valid → 200
2. add duplicate → 409
3. add full list → 409
4. delete valid → 200
5. delete unknown → 404
6. test valid → 200
7. test missing URL → 400
8. settings renders add URL input
9. settings renders remove buttons

### 4.4 Hybrid hardening imports
- Contract-lock route branch tables must mirror firmware handlers by name.
- Avoid vacuous assertions (no dynamic self-referential counts).
- Use fixture skip guards when group is fixture-specific.
- Keep Playwright function signatures clean (no unused test args).

### 4.5 Closure tasks
- Mark Phase D complete in architecture plan.
- Add closure note in phase-d implementation plan.
- Update aggregator setup workflow.
- Add changelog closure entry.
- Bump to v7.6.0.5 where expected.

### 4.6 Data flow trace
```text
UI or API test action
  → stateful mock route validation
  → mutate in-memory state for add/delete only
  → return contract-accurate response
  → subsequent requests observe new state
```

---

## 5) Do NOT (§3.6)
- Do not implement stateless add/delete mocks.
- Do not skip contract branch coverage.
- Do not hardcode group number from prompt text.

---

## 6) Critical Rules (§3.7)
All 35 rules apply. Emphasize mock fidelity, fixture rigor, signature cleanliness, and closure documentation completeness.

---

## 7) Runtime Mutation Safety Checklist
### Runtime Mutation Safety Checklist
- [ ] All satellite iteration loops use `runtime_satellite_count`, not `MAX_SATELLITES`
- [ ] All cache mutations (add/remove/clear) happen under `s_cache_mutex` (AGG_LOCK/AGG_UNLOCK)
- [ ] No code caches a satellite array index across function calls or requests
- [ ] Boot path remains unified: satellite boot + aggregator overlay, not a fork (LESSON-OPS-074)
- [ ] NVS empty/fallback path verified (first boot or corrupted namespace → compile-time defaults)
- [ ] NVS write errors logged but do not crash or corrupt runtime state
- [ ] Array compaction after delete tested (v7.6.0.2+)
- [ ] Poll task behavior verified during/after add/remove operations

---

## 8) Documentation Updates (§3.8)
Mandatory in this step:
- `Docs/v7.5-v7.6-architecture-plan.md`
- `Docs/phase-d-implementation-plan.md`
- `Docs/aggregator-setup.md`
- `Docs/changelog.md`

---

## 9) Validation & Regeneration (§3.9)
```bash
bash scripts/preflight.sh
FIXTURE_SET=3sensor npx playwright test --project=chromium
FIXTURE_SET=mixed npx playwright test --grep "Mixed" --project=chromium
FIXTURE_SET=system npx playwright test --grep "System" --project=chromium
FIXTURE_SET=aggregator npx playwright test --grep "Aggregator" --project=chromium
```

---

## 10) Device Testing (§3.10)
1. Add/remove/test via UI on hardware.
2. Reboot and verify persisted behavior.
3. Confirm no settings-panel regressions.

---

## 11) Contract-Lock (§3.12)
Enumerate and test all branches with response shapes for:
- `handle_add_satellite_()`
- `handle_delete_satellite_()`
- `handle_test_satellite_()`

Required branch set:
- Add: 200, 400(missing), 400(invalid), 409(full), 409(duplicate), 400(probe fail)
- Delete: 200, 404
- Test: 200, 400(missing), 400(unreachable)

---

## 12) Snippet Quality Gates (§3.13)
- Explicit route+method+status mapping.
- Deterministic response shape examples.
- Distinguish mutating vs read-only route behavior.

---

## 13) Test Group Guardrails (§3.11)
- Derive group number from source file.
- Use hardcoded expected counts where appropriate.
- Use fixture skip guards with clear reasons.
- Ensure CI path executes new group for relevant fixture set.

---

## 14) MANDATORY deliverables
1. Session log
2. Instruction Compliance Output table
3. Validation evidence + route branch coverage mapping
