# v7.6.0.5 Consolidated Audit & Lessons — PR #129

_Date: 2026-04-04_
_PR: [#129 — v7.6.0.5 — Playwright Tests + Phase D Closure](https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor/pull/129)_
_Agent: Claude Sonnet 4.5 (anthropic-code-agent)_
_Prompt: `prompts/phaseD/v7.6.0.5-implementation-instructions-for-coding-agent.md`_
_Branch: `claude/add-new-validation-tests`_
_Final shipped state reached: 2026-04-04T06:55:50Z (PR #129 merge)_

---

> **Canonical closure artifact for v7.6.0.5 and for Phase D as a whole.** This document is
> analogous to `prompts/phaseD/v7.6.0.4-PR126-PR128-consolidated-audit-and-lessons.md` for the
> prior step. It is based on first-hand inspection of: the session handoff
> (`prompts/handoff/session-handoff-v7.6.0.5.md`), the coding agent session log
> (`Docs/session-log-2026-04-04-v7.6.0.5.md`), PR #129 metadata and merged state, the live
> `tests/mock-server/server.js` and `tests/browser/dashboard.spec.js` at merge HEAD, and the
> `playwright.config.js` `webServer` block.

---

## Overview

- **PR:** [#129](https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor/pull/129)
- **Branch:** `claude/add-new-validation-tests`
- **Scope:** Phase D closure — Test Group 21 (19 tests) + stateful mock server for satellite management
- **Final commit:** `188aa40` (HEAD at merge, full SHA `188aa40a4421cd99269b32dacfc06f345ae40dfb`)
- **Validation result:** 402 tests passed / 0 failed across all 4 fixture sets (3sensor, mixed, system, aggregator)
- **Firmware changes:** none
- **Dashboard JS/HTML changes:** none
- **Phase D status:** ✅ **COMPLETE** — all six steps shipped and closed

---

## What Was Shipped

PR #129 comprised 9 commits on branch `claude/add-new-validation-tests`.
The 7 meaningful implementation commits are grouped below in chronological order.

### Initial implementation

| SHA | Purpose |
|-----|---------|
| *(initial commit — exact SHA not individually recorded in session log)* | Stateful mock server satellite state (`managedSatellites[]`, `initManagedSatellites()`), four management endpoint handlers (add, delete, test, reset), update to `/api/aggregator/gateways` for `FIXTURE_SET=aggregator`; Test Group 21 with 19 tests (12 API, 2 UI, 5 PR #128 regression) |

### Round 1 review fixes

| SHA | Purpose |
|-----|---------|
| `4da9431` | **Blocking fixes — auth + body guard + method 405:** Added `hasNonEmptyBody()` validation to all management POST routes; added `checkAuth(req)` to destructive endpoints (DELETE, test-satellite, reset-satellites); all endpoints now return 405 for wrong HTTP method; DELETE `/api/aggregator/satellite/` (empty ID) returns 400 |
| `f5b61d1` | **Test isolation + poll parameter + monotonic IDs:** Added `beforeEach` reset hook calling `/api/system/reset-satellites?auth=mock` before each Group 21 test; poll interval clamped to `[10, 3600]` matching firmware `strtol()` + range check; switched from length-based to `nextSatelliteId++` counter for monotonic, collision-safe IDs |
| `409da9a` | **Deterministic regression test rewrites:** Replaced flaky `waitForFunction` poll-rerender test with explicit wait; simplified test-satellite status assertion to verify non-empty meaningful content; delete regression test injects `sessionStorage.setItem('auth_token', 'mock')` to complete auth flow |

### Documentation closure

| SHA | Purpose |
|-----|---------|
| `e27581c` | Session log updated with Round 1 review fix results and Instruction Compliance Output table; closure checklist completed |

### Round 2 code fixes

| SHA | Purpose |
|-----|---------|
| `fa881fe` | **Response shape mismatch fix:** Mock `POST /api/aggregator/add-satellite` corrected from nested `{ok, satellite:{id,name,url,poll}}` to flat firmware contract `{ok, id, name, satellite_count}`; 3 test assertions updated to match; poll-rerender test wait changed from `waitForTimeout(2000)` to `page.waitForResponse()` on `/api/aggregator/gateways` |
| `f851f45` | **waitForTimeout replacements + parallelism comment:** Replaced 3 `waitForTimeout(1000)` instances in add/delete regression tests with `page.waitForResponse()` waiting on actual endpoints; completed delete regression test (add satellite first, stub credentials, handle dialog); added 3-line comment explaining `managedSatellites` shared state in `fullyParallel` execution |
| `188aa40` | Session log updated with Round 2 fix results and full validation evidence (402 passed / 0 failed) |

---

## Mock Server Contract (server.js)

The four satellite management endpoints in `tests/mock-server/server.js` mirror the firmware
handlers in `dashboard/sensor_history_multi.h`. This section is the canonical reference for
future agents writing tests against these endpoints.

---

### `POST /api/aggregator/add-satellite`

**Method:** POST
**Auth required:** ❌ No (intentional per firmware lines 3941–3946; constructive mutation is deliberately unauthenticated in Phase D)
**Content-Type required:** `application/x-www-form-urlencoded`, body `a=1`

**Validation branches (in evaluation order):**

| Branch | Condition | HTTP | Response body |
|--------|-----------|------|---------------|
| Empty body | `!hasNonEmptyBody(req)` | 400 | `{"ok":false,"message":"Non-empty body required for management POST","status":400}` |
| Wrong method | method ≠ POST | 405 | `{"ok":false,"message":"Method not allowed","status":405}` |
| Missing `url` | `url` param absent | 400 | `{"ok":false,"message":"Missing url parameter","status":400}` |
| Bad URL prefix | URL does not start with `http://` | 400 | `{"ok":false,"message":"URL must start with http://","status":400}` |
| URL too long | URL length ≥ 128 chars | 400 | `{"ok":false,"message":"URL too long","status":400}` |
| Capacity | `managedSatellites.length >= MOCK_MAX_SATELLITES (8)` | 409 | `{"ok":false,"message":"Max satellites reached","status":409}` |
| Duplicate | URL already in `managedSatellites` | 409 | `{"ok":false,"message":"Satellite already exists","status":409}` |
| Unreachable | URL contains `unreachable` substring (mock simulation) | 400 | `{"ok":false,"message":"Satellite unreachable or invalid manifest","status":400}` |
| **Success** | All validations pass | **200** | `{"ok":true,"id":"<generated>","name":"<name or url>","satellite_count":<N>}` |

**Success response shape (flat format — matches firmware `httpd_resp_sendstr` payload):**
```json
{"ok":true,"id":"sat-3","name":"My Satellite","satellite_count":3}
```

---

### `DELETE /api/aggregator/satellite/{id}`

**Method:** DELETE
**Auth required:** ✅ Yes — `?auth=mock` query parameter in tests (mirrors `authenticate_management_()` at firmware line 4080)

**Validation branches:**

| Branch | Condition | HTTP | Response body |
|--------|-----------|------|---------------|
| Wrong method | method ≠ DELETE | 405 | `{"ok":false,"message":"Method not allowed","status":405}` |
| Auth failure | `!checkAuth(req)` | 401 | `{"ok":false,"message":"Management authentication required","status":401}` |
| Empty ID | `{id}` segment is empty string | 400 | `{"ok":false,"message":"Missing satellite ID","status":400}` |
| Unknown ID | `id` not found in `managedSatellites` | 404 | `{"ok":false,"message":"Unknown satellite ID","status":404}` |
| **Success** | ID found and removed | **200** | `{"ok":true}` |

**Side effect on success:** Entry removed from `managedSatellites[]`. Subsequent `/api/aggregator/gateways` (under `FIXTURE_SET=aggregator`) reflects the deletion.

---

### `POST /api/aggregator/test-satellite`

**Method:** POST
**Auth required:** ✅ Yes — `?auth=mock` (mirrors firmware line 4163)
**Content-Type required:** `application/x-www-form-urlencoded`, body `a=1`

**Validation branches (in evaluation order):**

| Branch | Condition | HTTP | Response body |
|--------|-----------|------|---------------|
| Empty body | `!hasNonEmptyBody(req)` | 400 | `{"ok":false,"message":"Non-empty body required for management POST","status":400}` |
| Wrong method | method ≠ POST | 405 | `{"ok":false,"message":"Method not allowed","status":405}` |
| Auth failure | `!checkAuth(req)` | 401 | `{"ok":false,"message":"Management authentication required","status":401}` |
| Missing `url` | `url` param absent | 400 | `{"ok":false,"message":"Missing url parameter","status":400}` |
| Bad URL prefix | URL does not start with `http://` | 400 | `{"ok":false,"message":"URL must start with http://","status":400}` |
| URL too long | URL length > 200 chars | 400 | `{"ok":false,"message":"URL too long","status":400}` |
| Unreachable | URL contains `unreachable` substring | 400 | `{"ok":false,"message":"Satellite unreachable or invalid manifest","status":400}` |
| **Success** | All validations pass | **200** | `{"ok":true,"gateway":{"id":"...","name":"...","hardware":"...","sensor_count":N}}` |

**Success response shape (nested `gateway` object — matches firmware `httpd_resp_sendstr` payload):**
```json
{
  "ok": true,
  "gateway": {
    "id": "sat-mock-001",
    "name": "Test Gateway",
    "hardware": "ESP32-S3",
    "sensor_count": 1
  }
}
```

---

### `POST /api/system/reset-satellites`

**Method:** POST
**Auth required:** ✅ Yes — `?auth=mock` (mirrors firmware line 4260)

**Validation branches:**

| Branch | Condition | HTTP | Response body |
|--------|-----------|------|---------------|
| Wrong method | method ≠ POST | 405 | `{"ok":false,"message":"Method not allowed","status":405}` |
| Auth failure | `!checkAuth(req)` | 401 | `{"ok":false,"message":"Management authentication required","status":401}` |
| **Success** | Always succeeds after auth | **200** | `{"ok":true,"satellite_count":<N after reset>}` |

**Side effect on success:** Calls `initManagedSatellites()` which reloads `managedSatellites[]`
from the `aggregator-gateways.json` fixture defaults. Used as the `beforeEach` reset hook in
Group 21 to guarantee test isolation.

---

## Test Group 21 Structure

**Group name:** `21. Satellite Management`
**Location:** `tests/browser/dashboard.spec.js` lines 1613–1819
**Skip guard:** `test.beforeEach` skips when `process.env.FIXTURE_SET !== 'aggregator'`
**Reset hook:** `test.beforeEach` calls `POST /api/system/reset-satellites?auth=mock` before each test
**Total tests:** 19

### API Tests (12 tests) — fixture argument: `{ request }`

| # | Test name |
|---|-----------|
| 1 | `POST add-satellite: valid URL returns 200` |
| 2 | `POST add-satellite: duplicate URL returns 409` |
| 3 | `POST add-satellite: missing URL returns 400` |
| 4 | `POST add-satellite: full list returns 409` |
| 5 | `POST add-satellite: unreachable URL returns 400` |
| 6 | `DELETE satellite: valid ID returns 200` |
| 7 | `DELETE satellite: unknown ID returns 404` |
| 8 | `POST test-satellite: valid URL returns gateway info` |
| 9 | `POST test-satellite: missing URL returns 400` |
| 10 | `POST test-satellite: unreachable URL returns 400` |
| 11 | `POST test-satellite: URL without http:// returns 400` |
| 12 | `POST reset-satellites: resets to fixture defaults` |

### UI Tests (2 tests) — fixture argument: `{ page }`

| # | Test name |
|---|-----------|
| 13 | `Settings panel renders add form (URL input, Add button, Test button visible)` |
| 14 | `Settings panel renders remove buttons for each satellite` |

### Regression Tests (5 tests, MANDATORY) — fixture argument: `{ page }`

These tests guard specifically against the BUG-080 / BUG-081 regression class from PR #128.

| # | Test name | Bug class guarded |
|---|-----------|------------------|
| 15 | `URL input value preserved across poll-driven rerender` | BUG-080 — `pollAggregatorLive()` rerender guard while user is typing |
| 16 | `test-satellite result appears in live panel after action` | BUG-081 — re-query by ID after async auth boundary |
| 17 | `settings panel not destroyed during in-flight add` | BUG-080 — `_satAddInFlight` guard prevents destructive rebuild |
| 18 | `panel remains usable after completed add` | BUG-080/081 — panel accepts new input after successful add |
| 19 | `panel remains usable after completed delete` | BUG-081 — Settings panel renders correctly after delete completes |

---

## Review Issues and Resolutions

All issues are sourced from the original four PR reviews (Copilot, CODEX, GPT, human/operator) plus the Round 2 issues identified post-initial-review.

| Issue | Severity | Root Cause | Fix Applied | Commit |
|-------|----------|-----------|-------------|--------|
| Mock endpoints accepted POST with empty body | Blocking | `hasNonEmptyBody()` helper not implemented; firmware guard at lines 2381–2384 not mirrored | Added `hasNonEmptyBody()` validation as first check on all management POST routes | `4da9431` |
| DELETE, test-satellite, and reset-satellites had no auth enforcement | Blocking | `checkAuth(req)` not implemented; firmware `authenticate_management_()` calls not mirrored | Added `checkAuth(req)` to all three auth-required endpoints; `add-satellite` intentionally exempt per firmware lines 3941–3946 | `4da9431` |
| Wrong HTTP method returned 404 instead of 405 | Blocking | Mock used generic 404 for method mismatches; firmware sends 405 for wrong methods at lines 3936–3939, 4075–4078, 4159–4162, 4256–4259 | All endpoints now return 405 `{"ok":false,"message":"Method not allowed","status":405}` | `4da9431` |
| `DELETE /api/aggregator/satellite/` (empty ID) returned 404 instead of 400 | Blocking | Missing empty-ID guard; firmware guard at lines 4086–4089 returns 400 | Empty satellite ID path now returns 400 | `4da9431` |
| Cross-test state contamination from shared `managedSatellites` | High | No `beforeEach` reset; tests that mutate state (add, delete) left dirty state for subsequent tests | Added `beforeEach` reset calling `POST /api/system/reset-satellites?auth=mock` before each Group 21 test | `f5b61d1` |
| Poll interval accepted any value; no range clamp | High | `gw.poll` used directly from fixture; firmware `strtol()` + range check at lines 4007–4012 not mirrored | Poll interval now clamped to `[10, 3600]` with default 30 | `f5b61d1` |
| ID generation used `managedSatellites.length` — IDs recycled after deletions | High | Length-based ID resets after remove, can produce duplicate IDs in parallel tests | Switched to `nextSatelliteId++` monotonic counter | `f5b61d1` |
| Regression test 15 (poll-rerender) used flaky `waitForFunction` | High | `waitForFunction` polling was race-prone on slow CI | Simplified to `waitForTimeout(2000)` then later upgraded to `page.waitForResponse()` (Round 2) | `409da9a` → `fa881fe` |
| `add-satellite` success response shape was nested `{ok, satellite:{id,name,url,poll}}` | Blocking | Mock response shape not cross-checked against firmware `httpd_resp_sendstr` payload; prompt example differed from firmware | Corrected to flat format `{ok, id, name, satellite_count}` matching firmware | `fa881fe` |
| Poll-rerender test used `waitForTimeout(2000)` waiting on wrong event | High | Aggregator mode polls `/api/aggregator/gateways`, not `/api/aggregator/live`; timeout was non-deterministic | Changed to `page.waitForResponse()` on `/api/aggregator/gateways` | `fa881fe` |
| Add/delete regression tests used `waitForTimeout(1000)` fixed delays | High | Fixed delays are non-deterministic in CI; response-gated waits are reliable | Replaced 3 `waitForTimeout` calls with `page.waitForResponse()` on actual API endpoints | `f851f45` |
| Delete regression test did not complete the delete operation | High | Test stubbed auth credentials but didn't handle the dialog, so the delete never executed | Added satellite first, stubbed `requestManagementCredentials` before click, added `page.on('dialog')` handler | `f851f45` |
| No documentation of `managedSatellites` shared-state semantics under `fullyParallel` | Low | Implicit assumption about worker isolation not documented | Added 3-line comment explaining `fullyParallel` worker isolation and within-worker state sharing | `f851f45` |

---

## Lessons Learned

The following lessons have been added to  `Docs/bugs-and-lessons-learned.md`

---

### LESSON-OPS-112 — Response shape mismatch between mock and firmware contract (2026-04-04)

**Context:** PR #129 Round 2 — Mock `POST /api/aggregator/add-satellite` returned nested
`{ok, satellite:{id,name,url,poll}}`. The firmware handler's `httpd_resp_sendstr` payload is flat:
`{ok, id, name, satellite_count}`. Three test assertions also expected the wrong shape. The
mismatch survived all four review passes before being caught in the Round 2 review.

**Rule:** When implementing a mock endpoint, always locate the firmware handler's literal
`httpd_resp_sendstr(...)` call and copy the response JSON from there — not from the prompt
description, not from a prior audit table, not from memory. If any prompt example differs from
the live firmware string, the **firmware wins**. Verify both the field names and the nesting level
(flat vs nested). Tests that assert only status codes without checking response body shape will
miss this class of mismatch entirely.

**Scope:** Applies to all mock implementations in `tests/mock-server/server.js` and any future
mock infrastructure added in Phase 7.

---

### LESSON-OPS-113 — `page.waitForResponse()` with URL predicate is always preferable to `waitForTimeout()` for network-triggered state changes (2026-04-04)

**Context:** PR #129 initial implementation used `waitForTimeout(2000)` to wait for a poll cycle
to rerender the Settings panel. Round 1 review replaced it with `waitForTimeout(2000)` on the
grounds of simplicity. Round 2 review then correctly replaced all `waitForTimeout` calls with
`page.waitForResponse(url => url.includes('/api/aggregator/gateways'))` because:

1. The timeout is arbitrary — it may pass vacuously on fast CI or fail spuriously on slow CI.
2. The `waitForResponse()` call guarantees the network round-trip has completed, meaning the
   dashboard has received the response and had the opportunity to update state.
3. `waitForResponse()` is no more complex to write and is self-documenting about what event the
   test is actually waiting for.

**Rule:** Never use `waitForTimeout(N)` as a proxy for "wait for a network response to arrive."
Use `page.waitForResponse(urlPredicate)` instead. `waitForTimeout` is only appropriate when:
- testing that something does NOT happen within a time window
- adding intentional breathing room between user-input actions that are not network-gated

**Exception:** `waitForTimeout` may be used in regression tests that verify timing behavior
(e.g., confirming an action does not fire prematurely), but must be accompanied by a comment
explaining why a response-based wait is not sufficient.

---

### LESSON-OPS-114 — When stubbing `window.requestManagementCredentials` in Playwright tests, stub before the click (2026-04-04)

**Context:** PR #129 Round 2 fix for the delete regression test (test 19). The original test
structure was:
1. Click the Remove button
2. `page.evaluate(() => window.requestManagementCredentials = ...)` to stub the function

This ordering failed because `dashboard.js` may invoke `requestManagementCredentials` synchronously
on the click event — before the `evaluate` promise resolves. The function stub never took effect,
the auth dialog was shown instead, the test timed out.

**Fix:** Stub `window.requestManagementCredentials` with `page.evaluate()` **before** triggering
any click that could invoke it. The correct sequence is:
1. `await page.evaluate(() => { window.requestManagementCredentials = ... })`
2. `page.on('dialog', handler)` if a browser dialog may also appear
3. Click the button

**Rule:** Any Playwright test that needs to stub a JS function called in response to a user
interaction must complete the stub BEFORE the interaction. Async evaluation (`page.evaluate`)
is not instantaneous — the click handler may fire between the click and the evaluate completing.
Stub-then-click is the only ordering that is deterministically safe.

**Applies to:** All Playwright tests that stub `window.requestManagementCredentials`,
`window.confirm`, or any other synchronously-invoked window function in `dashboard.js`.

---

## Phase D Complete — Shipped State Reference

This table is the single source of truth for the Phase D implementation sequence. Use it in
future session handoffs and audit documents.

| Version | Scope | PR(s) | Merge date | Final branch / commit |
|---------|-------|-------|------------|----------------------|
| v7.6.0.0 | NVS satellite persistence layer — `s_satellites[]`, `s_sat_count`, NVS load/save, `MAX_SATELLITES = 8`, `reset-satellites` stub, aggregator poll-task integration | #99 + #101 (fixup) | 2026-03-29 | `main` |
| v7.6.0.1 | `POST /api/aggregator/add-satellite` firmware endpoint — URL validation, probe, NVS persist, 400/409/200 responses | #108 + #110 (fixup) | 2026-03-31 | `main` |
| v7.6.0.2 | `DELETE /api/aggregator/satellite/{id}` firmware endpoint — auth, ID lookup, NVS persist, 400/401/404/200 | #114 + #116 (fixup) | 2026-04-02 | `main` |
| v7.6.0.3 | `POST /api/aggregator/test-satellite` firmware endpoint — auth, URL validation, manifest probe, 400/401/200 | #119 + #121 (fixup) | 2026-04-02 | `main` |
| v7.6.0.4 | Dashboard add/remove/test satellite UI — interactive `renderSettingsPanel()`, async-safe status writes, poll rerender guards | #126 (feature) + #128 (stability) | 2026-04-04 | `main` — see `prompts/phaseD/v7.6.0.4-PR126-PR128-consolidated-audit-and-lessons.md` |
| **v7.6.0.5** | **Playwright Test Group 21 (19 tests) + stateful mock server — Phase D closure** | **#129** | **2026-04-04** | **`main` — HEAD `188aa40`** |

---

## Open Items / Deferred

### OI-001 — `managedSatellites` parallelism comment accuracy

**Current state:** `tests/mock-server/server.js` lines 92–95 contain a comment added in commit
`f851f45` that reads (paraphrased): *"each worker gets its own mock server instance so
`managedSatellites` state is per-worker."*

**Finding:** This assumption is **incorrect** for the current `playwright.config.js` configuration.
The `webServer` block uses a single `command`:

```js
webServer: {
  command: 'node tests/mock-server/server.js --port 3737',
  port: 3737,
  reuseExistingServer: !process.env.CI,
  ...
}
```

A single `webServer` entry spawns exactly one server process bound to port 3737. All Playwright
workers connect to this single shared instance. Workers do **not** each get their own mock server.

The current Group 21 tests are safe only because of the `beforeEach` reset hook
(`POST /api/system/reset-satellites?auth=mock`) that restores deterministic baseline state before
each test. The reset hook is load-bearing for test isolation; the "per-worker isolation" comment
is misleading documentation.

**Required fix (low priority — before Group 21 grows further):**

Update the comment in `server.js` to accurately describe the actual isolation model:

```js
// Shared state — all Playwright workers connect to this single server instance.
// Test isolation is maintained by the beforeEach reset hook in Group 21
// (POST /api/system/reset-satellites) which restores deterministic baseline state
// before each test. Do not assume worker-level isolation for managedSatellites.
```

**Priority:** Low. The tests are currently correct because of the reset hook. The inaccurate
comment becomes a risk only if a future test author removes the reset hook on the assumption
that per-worker isolation makes it redundant.

**Owner:** Any agent working on the first Phase 7 PR that touches `server.js` or Group 21.

---

_End of v7.6.0.5 PR #129 consolidated audit._
