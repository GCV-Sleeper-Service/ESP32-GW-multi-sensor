# v7.6.0.4 Consolidated Audit & Lessons — PR #126 + PR #128

_Date: 2026-04-04_
_PRs: [#126 — v7.6.0.4 — Dashboard Add/Remove/Test Satellite UI (Phase D Step 4)](https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor/pull/126) + [#128 — Post-merge regression fixes](https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor/pull/128)_
_Agents: Copilot SWE Agent (initial implementation + PR #128), Claude Anthropic Code Agent (fixup commits inside PR #126)_
_Prompt: `prompts/phaseD/v7.6.0.4-implementation-instructions-for-coding-agent.md`_
_Final shipped state reached: 2026-04-04T01:32:24Z (PR #128 merge)_

---

> **Canonical closure artifact for v7.6.0.4.** This document supersedes all prior partial or draft audit documents for this step. It is based on first-hand inspection of: the session handoff, the coding agent prompt, the original PR #127 draft audit, `Docs/session-log-2026-04-03-v7.6.0.4.md`, `Docs/bugs-and-lessons-learned.md`, PR #126 metadata and merged state, and PR #128 metadata and merged patch set.

---

## 1. Executive Summary

**Execution quality: MEDIUM-HIGH — the intended v7.6.0.4 feature shipped successfully, but the production-ready state was reached only after PR #128.**

PR #126 delivered the core Step 4 feature correctly in scope: the read-only Settings-panel satellite list was replaced with an interactive Add / Test / Remove UI, the correct management endpoints were used, the auth policy was correctly implemented, `dashboard.html` was mirrored, and generated artifacts were refreshed. Inside PR #126, two fixup commits corrected the `escHtml()` + `textContent` issue, the `safeJsonResponse()` omission in `_refreshSettingsPanel()`, and the `device_count → sensor_count` fallback in the test-success message.

After PR #126 merged, browser and device follow-up exposed an additional regression class: the Settings panel was not robust against async auth flows coexisting with poll-driven panel re-renders. PR #128 fixed that shipped-state gap by:

1. preventing destructive click propagation from the Settings panel buttons,
2. re-querying the live status DOM node after async auth boundaries,
3. guarding `pollAggregatorLive()` from re-rendering the Settings panel while satellite operations are in flight or while the user is actively editing the form.

The correct final judgment is:

- **PR #126** delivered the feature
- **PR #128** made the feature reliable in the real shipped environment
- the true Phase D Step 4 baseline for v7.6.0.5 is **PR #126 + PR #128 together**

---

## 2. Delivery Statistics

| Metric | Value |
|--------|-------|
| Primary feature PR | #126 |
| Follow-up fix PR | #128 |
| PR #126 merged | 2026-04-03T19:05:44Z |
| PR #128 merged | 2026-04-04T01:32:24Z |
| PR #126 commits | 4 |
| PR #128 commits | 4 |
| PR #126 changed files | 25 |
| PR #128 changed files | 4 |
| PR #126 additions / deletions | 3,937 / 3,350 |
| PR #128 additions / deletions | 2,745 / 2,572 |
| Functional files in final shipped state | `dashboard/dashboard.js`, `dashboard/dashboard.html`, `dashboard/dashboard.h`, `Docs/bugs-and-lessons-learned.md` |
| Firmware changes | none |
| New API endpoints | none |
| New dashboard UI surface | Add Satellite form, Test button, Add button, per-satellite Remove button, enhanced satellite status rows |

---

## Part I — Did the Coding Agent Deliver Properly and Accurately What Was Required?

**Yes — but only when the shipped state is evaluated as PR #126 plus PR #128.**

### What PR #126 delivered correctly

1. `renderSettingsPanel()` replaced with an interactive Settings panel
2. Add Satellite form added (URL input, optional name input, Test button, Add button, inline status)
3. Per-satellite Remove buttons added
4. Per-satellite status rows enhanced (`last_seen`, `consecutive_failures`, reachable/unreachable indicator)
5. `POST /api/aggregator/test-satellite` used for test
6. `POST /api/aggregator/add-satellite` used for add
7. `DELETE /api/aggregator/satellite/{id}` used for remove
8. `dashboard.html` mirrored all JS changes
9. `dashboard.h` regenerated
10. No firmware code touched

### What PR #126 did not finish cleanly on first pass

Three implementation defects required correction before the merge result was clean:

- `escHtml()` values were written through `textContent` in status paths — corrected to use `innerHTML`
- `_refreshSettingsPanel()` used `r.json()` instead of `safeJsonResponse()`
- test-success line used `sensor_count` only without the established `device_count → sensor_count` fallback

All three were fixed before PR #126 merged.

### Why PR #128 must be counted as part of the delivery assessment

The Step 4 feature was not fully stable after PR #126 merged. PR #128 fixed a real shipped-state regression class:

- form inputs were wiped during active interaction
- auth-confirmed test flows completed without any visible status update because the stored DOM reference had gone stale
- periodic re-rendering could still tear down the Settings panel while the user was interacting with it

Correct delivery assessment:

> The coding agent delivered the required feature, but the final accepted implementation is the sum of PR #126 and PR #128. Evaluating PR #126 in isolation understates the real shipped lessons and overstates closure readiness.

---

## Part II — Did the Codebase State Match the Prompt's Assumptions?

**Mostly yes, with four real prompt-robustness gaps.**

### What matched

1. All three management endpoints already existed and were usable from dashboard JS
2. The step was correctly scoped as dashboard/UI-only work
3. `requestManagementCredentials()`, `safeJsonResponse()`, `escHtml()`, and `escAttr()` already existed for reuse
4. `renderSettingsPanel()` was a read-only placeholder ready to be replaced
5. JS ↔ HTML mirror workflow was correctly emphasized as a high-risk area

### Real prompt/codebase robustness gaps exposed by the shipped result

#### M1 — Async DOM volatility was not elevated enough

The prompt did not clearly force the agent to treat Settings-panel DOM as unstable across:

- auth modal waits
- poll-driven re-renders
- any other `innerHTML` refresh path

This gap mattered because the code captured a DOM element reference before an async auth flow and later wrote to a stale node.

#### M2 — Live DOM re-query discipline was not made explicit

The prompt did not explicitly require:

> if async callbacks update UI status after a possible panel rebuild, re-query the live element by stable `id` before writing.

PR #128 had to add exactly that pattern.

#### M3 — Poll-driven re-render guardrails were under-specified

The prompt did not explicitly require the Settings panel to skip re-render while:

- `_satTestInFlight`, `_satAddInFlight`, or `_satRemoveInFlight` is true
- the user is actively focused in the URL/name inputs

PR #128 added that runtime guard in `pollAggregatorLive()`.

#### M4 — The v7.6.0.5 prompt became stale after PR #128

The base v7.6.0.5 prompt was written before the final shipped state settled. It therefore requires the corrections now incorporated into `prompts/phaseD/v7.6.0.5-implementation-instructions-for-coding-agent.md`:

- PR #126 + PR #128 together as the v7.6.0.5 baseline
- Explicit regression coverage requirements for the PR #128 bug class
- Firmware-over-example contract lock for mock routes

---

## Part III — What Implementation Decisions Did the Agent Make Beyond the Prompt?

### PR #126 implementation decisions beyond the explicit prompt text

| Decision | Assessment |
|----------|------------|
| Module-level in-flight flags for Test/Add/Remove | Correct and consistent with existing dashboard patterns |
| `_refreshSettingsPanel()` as the single re-fetch path after state-changing actions | Correct |
| Inline remove-status element per satellite card | Reasonable UX extension |
| Silent refresh-failure catch in `_refreshSettingsPanel()` | Functional but reduces debuggability |
| Explicit per-field rendering for `last_seen`, `consecutive_failures`, reachable state | Correct |

### PR #128 implementation decisions beyond the original prompt

| Decision | Assessment |
|----------|------------|
| `e.stopPropagation()` on Test/Add/Remove button handlers | Correct fix for click propagation issue |
| Capture URL value synchronously before async auth (`capturedUrl` pattern) | Correct |
| Re-query `document.getElementById('sat-add-status')` in async callbacks — not a captured pre-auth node | Correct and important |
| Treat auth cancellation as `AUTH_CANCELLED` sentinel to avoid overwriting "Test cancelled" with generic failure | Correct |
| Guard `pollAggregatorLive()` from re-rendering the Settings panel while inputs are focused or ops are in flight | Correct and strategically important |
| Mirror all JS changes to `dashboard.html` and regenerate `dashboard.h` | Required by LESSON-OPS-043 |

### Documentation decisions in PR #128

PR #128 updated `Docs/bugs-and-lessons-learned.md` to record the regression class as:

- **BUG-080** — fields cleared during Test/Add interaction
- **BUG-081** — auth dialog resolves but no status update appears
- **LESSON-OPS-111** — captured DOM references become stale across `innerHTML` re-renders

These are part of the final shipped-state lesson set and must be carried forward into v7.6.0.5.

---

## Part IV — Coding Agent Prompt Audit Against the Writing Guide

### Where the v7.6.0.4 prompt aligned well

1. Required reading was concrete and file-specific
2. Auth policy by endpoint was clearly enumerated
3. The prompt strongly emphasized LESSON-OPS-043 (JS ↔ HTML mirror)
4. The prompt explicitly prohibited firmware changes and new endpoints
5. The prompt included concrete quality gates (`escHtml()`, `escAttr()`, `safeJsonResponse()`, in-flight guards, no inline handlers)

### Where the final shipped result shows the prompt needed strengthening

#### G-Audit-1 — Async UI lifecycle safety needed to be first-class, not implicit

For v7.6.0.4, once an auth modal and poll-driven panel refresh coexisted, async DOM staleness should have been a first-class risk in the prompt. It was not.

#### G-Audit-2 — Re-render suppression rules needed explicit expression

The prompt should have told the agent:

- do not re-render the Settings panel while satellite operations are in flight
- do not re-render the Settings panel while the add-form inputs are focused

#### G-Audit-3 — Prompt audits must distinguish real prompt defects from implementation drift

Audit documents must identify prompt defects only when the exact missing or contradictory text has been re-verified in the actual prompt file. Claiming a prompt omitted something it already contained is an analytical error that must not be carried forward.

### Carry-forward prompt-writing improvements

1. Add an **Async DOM Safety** block to dashboard prompts: if callbacks run after auth/network waits, re-query stable `id` nodes before updating UI
2. Add a **Re-render Guard** rule for poll-driven dashboards: skip panel rebuild while form inputs are focused or action flags are in flight
3. Add an explicit **Shipped-State Closure Rule**: if a post-merge fix PR changes the real shipped behavior, the consolidated audit must cover both PRs
4. Add an **Audit Evidence Rule**: do not identify a prompt defect unless the exact missing/contradictory text has been re-verified in the prompt file being audited
5. Do not mint new `LESSON-OPS-*` IDs in audit-only documents unless the canonical lessons file is updated in lockstep

---

## Part V — PR Comment Review and Disposition

### PR #126 code-review findings

| Finding | Reviewer source | Disposition |
|---------|----------------|-------------|
| `escHtml()` + `textContent` misuse in status paths | Gemini / Copilot / GPT / Codex / Perplexity | Fixed in PR #126 fixup |
| `_refreshSettingsPanel()` should use `safeJsonResponse()` | Gemini / Copilot / GPT / Codex / Perplexity | Fixed in PR #126 fixup |
| test-success line should follow `device_count → sensor_count` fallback | Gemini / GPT / Codex / Perplexity | Fixed in PR #126 fixup |

### PR #128 fix scope

| Area | Disposition |
|------|-------------|
| Auth-cancel / status-path stability | Fixed |
| Click interaction / event propagation | Fixed |
| Rerender guard against focused inputs / in-flight ops | Fixed |
| Mirrored HTML parity | Fixed |
| Canonical bug/lesson documentation update | Fixed |

---

## Part VI — Fixes Implemented

### PR #126 — initial implementation

- Interactive Settings panel delivered
- Add/Test/Remove flows, enhanced satellite status rows, endpoint integration
- `dashboard.html` mirror, `dashboard.h` regeneration

### PR #126 — internal fixup commits (before merge)

1. `escHtml()` status rendering corrected to use `innerHTML`
2. `_refreshSettingsPanel()` corrected to use `safeJsonResponse()`
3. test-success device count corrected to follow `device_count → sensor_count` fallback

### PR #128 — post-merge regression fixes

1. Button click handlers call `e.stopPropagation()`
2. Test flow captures URL synchronously before async auth work begins
3. Status updates re-query the live DOM node after async boundaries
4. Auth-cancel is handled via `AUTH_CANCELLED` sentinel — not overwritten by generic failure
5. `pollAggregatorLive()` skips Settings-panel re-render while any action flag is true or inputs are focused
6. `dashboard.html` mirrored to match all JS fixes
7. `dashboard.h` regenerated
8. `Docs/bugs-and-lessons-learned.md` updated with BUG-080, BUG-081, LESSON-OPS-111

---

## Part VII — Remaining Issues and Deferred to v7.6.0.5

### R1 — No Playwright lock-in yet for PR #128 regression class

The shipped code contains the correct guards and stale-DOM fixes, but those paths are not yet locked down by dedicated browser tests. This is the primary v7.6.0.5 deliverable.

### R2 — Stateful mock coverage still needed

The add/remove/test interactions need stateful mock-server behavior to properly test list mutation, reset-to-baseline, and UI persistence after actions.

---

## Part VIII — Device Testing Checklist

The checklist below captures the final verification set for the fully shipped PR #126 + PR #128 state.

| Test | Expected outcome |
|------|-----------------|
| Open Settings panel | Add Satellite form renders correctly |
| Type URL and friendly name, wait through poll interval | Fields are preserved while editing (PR #128 rerender guard) |
| Click Test, cancel auth | Inline "Test cancelled" status remains visible |
| Click Test, complete auth with valid URL | "Testing…" then success result appears in live panel |
| Click Test with invalid URL | Inline failure message appears |
| Click Add with valid URL | New satellite appears in list |
| Click Remove and confirm | Satellite disappears from list |
| During any active add/test/remove op | Settings panel is not destructively re-rendered |
| Dark/light mode | New inputs and buttons render correctly in both themes |

### Evidence posture

The existence of PR #128 is evidence that real-world post-merge verification found real regressions. v7.6.0.5 must preserve this knowledge in automated Playwright form so the regression class cannot silently return.

---

## Part IX — New Lessons

### Canonical lessons now governing this area

All three entries are present in `Docs/bugs-and-lessons-learned.md` after PR #128:

- **BUG-080** — fields cleared during Test/Add interaction
- **BUG-081** — auth dialog resolves but no live status update appears
- **LESSON-OPS-111** — captured DOM references become stale across `innerHTML` re-renders

### Audit-process lessons from this closure

1. A consolidated audit must reflect the **final shipped state**, not just the first merged PR
2. Audit documents must not create canonical lesson IDs independently of the main lessons file
3. Prompt-defect claims must be re-verified against the exact prompt text before being published in an audit
4. Short supplements are not a substitute for a full consolidated audit when the handoff explicitly requires the established format

---

## Part X — Security Convention Summary (Phase D Dashboard Management)

| Action | Endpoint | Method | Auth required |
|--------|----------|--------|--------------|
| Test satellite | `/api/aggregator/test-satellite` | POST | ✅ Yes |
| Add satellite | `/api/aggregator/add-satellite` | POST | ❌ No |
| Remove satellite | `/api/aggregator/satellite/{id}` | DELETE | ✅ Yes |

This convention was stable through PR #126 and PR #128. PR #128 fixed UI lifecycle behavior only — endpoint security policy was not changed.

---

## Part XI — Lessons Carried Forward to v7.6.0.5

1. Treat **PR #126 + PR #128** as the only correct baseline for Phase D closure
2. Build stateful mock routes from the **live firmware contract** (`dashboard/sensor_history_multi.h`), not from prompt examples
3. Add Playwright coverage for:
   - add/delete/test success and failure paths
   - input preservation across rerender opportunities (BUG-080 class)
   - live status updates after auth flow completion (BUG-081 class)
   - rerender suppression while inputs are focused or actions are in flight
   - continued panel usability after each action type completes
4. The revised v7.6.0.5 coding agent prompt (`prompts/phaseD/v7.6.0.5-implementation-instructions-for-coding-agent.md`) incorporates all corrections from this audit
5. This document is the sole canonical closure reference for v7.6.0.4

---

_End of v7.6.0.4 PR #126 + PR #128 consolidated audit._