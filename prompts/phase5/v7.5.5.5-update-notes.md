# v7.5.5.5 — Update Notes (read BEFORE the main prompt)

_Date: 2026-03-25_
_Applies to: `v7.5.5.5-implementation-instructions-for-coding-agent.md`_
_Context: v7.5.5.3 hotfix + hotfix-2 merged, BUG-064 through BUG-069_

---

## §2 Required Reading — ADD these files:

```
7. Docs/session-log-2026-03-25-v7553-hotfix.md — hotfix session log (BUG-064–069)
8. Docs/architecture-revision-and-action-plan.md — design principles and action plan
9. Docs/aggregator-satellite-gateway-principles.txt — user design principles
```

---

## §3 Current Status — REPLACE with:

```
- v7.5.5.4 complete and merged (aggregator Playwright tests pass)
- v7.5.5.3-hotfix and hotfix-2 merged: BUG-064–069 fixed, LESSON-OPS-074 added
- Unified boot path established (aggregator = satellite + overlay)
- New DOM structure: Gateways section separate from SENSORS section
- Generator builds gateway_meta from board profile (chip_variant → hardware string)
- Environmental chart sections hidden when no env sensors exist
- All CI checks pass on main
```

---

## §4 Pre-condition Checks — FIX bare commands (Rule 5 violation):

Replace:
```bash
npx playwright test --project=chromium
npx playwright test --project=firefox
```

With:
```bash
FIXTURE_SET=3sensor npx playwright test --project=chromium
FIXTURE_SET=3sensor npx playwright test --project=firefox
```

---

## §5a aggregator-setup.md — ADD these sections:

The aggregator setup guide should include:

- **Naming convention:** `sat-{chip}-{flash}m-{location}` / `agg-{chip}-{flash}m-{location}`
- **Config separation:** `gateway.json` → `sensors_file` for per-device sensor config
- **Board content correctness:** Dashboard shows board-specific info only (no C3 content on S3)
- **Gateway names:** Should be plain text (no HTML special characters)
- **Dashboard layout:** Two sections — GATEWAYS (satellite tabs, summary, per-gateway views, settings) and SENSORS (local sensors on this device)
- **Important:** `config/gateway.json` and `config/aggregator.json` must be absent when running `render_sensor_config.py --write` for CI validation. They are deployment configs, not CI configs.

---

## §5b Architecture plan updates — ADD:

Update `Docs/v7.5-v7.6-architecture-plan.md` Section 11 Phase 5 status:
- Mark all v7.5.5.x steps complete
- Add note: "The aggregator boot path was redesigned as a unified pipeline
  (BUG-064/LESSON-OPS-074). The forked if/else from v7.5.5.3 was replaced
  with a single pipeline + overlay model in the hotfix."
- Note Phase D (runtime satellite management) as next milestone (v7.6.0.x)

---

## §5c Closure gate — ADD to the checklist:

```
- [ ] All fixture JSON files validated with `python3 -m json.tool` (no errors)
- [ ] `python3 scripts/render_sensor_config.py --check` passes (with gateway.json ABSENT)
- [ ] BUG-064 through BUG-069 documented in bugs-and-lessons-learned.md
- [ ] LESSON-OPS-074 documented
- [ ] Session logs exist for hotfix and hotfix-2
- [ ] Critical Rule 26 (LESSON-OPS-074) in prompt-index-and-workflow.md
```

---

## §5d bugs-and-lessons — ADD verification:

Verify these entries exist (added by hotfix sessions):
- BUG-064: Aggregator boot path skips satellite pipeline
- BUG-065: Gateway cards in SENSORS section
- BUG-066: Remote satellite "calculating..." history
- BUG-067: C3 content on non-C3 boards
- BUG-068: Manifest hardware hardcoded "ESP32-C3"
- BUG-069: Env chart sections visible with no env sensors
- LESSON-OPS-074: Aggregator boot = satellite + overlay

---

_End of update notes. Now read the main v7.5.5.5 prompt._
