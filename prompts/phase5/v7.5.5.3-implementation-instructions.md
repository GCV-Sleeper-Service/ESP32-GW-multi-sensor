# v7.5.5.3 — Phase 5 Step 3

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.5.2 complete (aggregator API endpoints working)
- /api/aggregator/gateways and /api/aggregator/live return data (confirm)
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase5-implementation-plan.md` (v7.5.5.3 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/v7.5-v7.6-architecture-plan.md` (Section 8 — Gateway Aggregation)
4. `dashboard/dashboard.js` — current boot flow and manifest loading
5. `dashboard/dashboard.html` — UI structure
6. Latest session log(s)

---

## Exact scope

Extend dashboard with multi-gateway UI.

1. Dashboard detects aggregator mode by checking /api/aggregator/gateways (404 = satellite mode)
2. Add gateway selector bar (tabs/dropdown per satellite)
3. 'All Gateways' summary view with online/offline status
4. Per-gateway view shows devices rendered with existing card renderers
5. Stale indicators for unreachable satellites
6. Namespaced device IDs (gw-id.device-id) to avoid collisions
7. Satellite-mode dashboard COMPLETELY UNAFFECTED
8. Mirror to dashboard.html, regenerate dashboard.h

**⚠️ HIGH RISK — significant dashboard changes. Must not break satellite mode.**

---

## Post-merge tag

```bash
git tag -a v7.5.5.3 -m "Phase 5 Step 3: Aggregator dashboard UI"
git push origin v7.5.5.3
```
