# v7.5.5.4 — Phase 5 Step 4

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.5.3 complete (aggregator dashboard UI working)
- Dashboard detects aggregator mode, shows gateway selector (confirm)
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase5-implementation-plan.md` (v7.5.5.4 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/v7.5-v7.6-architecture-plan.md` (Section 8 — Gateway Aggregation)
4. `tests/browser/dashboard.spec.js` — existing test patterns
5. `tests/mock-server/server.js` — mock route patterns
6. Latest session log(s)

---

## Exact scope

Create aggregator test fixtures and Playwright tests.

1. Create `tests/fixtures/variants/aggregator/` with mock satellite data
2. Add aggregator endpoint mocking to `server.js`
3. Add Group 17 tests: aggregator detection, gateway selector, device rendering, stale indicators, history proxy, satellite mode fallback
4. Update changelog, session log, bump to 7.5.5.4

---

## Post-merge tag

```bash
git tag -a v7.5.5.4 -m "Phase 5 Step 4: Aggregator Playwright tests"
git push origin v7.5.5.4
```
