# v7.5.5.0 — Phase 5 Step 0

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- Phase 4 complete (v7.5.4.4 merged)
- Dashboard shows environmental + network cards driven by manifest
- Moving to Phase 5: Aggregator MVP
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase5-implementation-plan.md` (v7.5.5.0 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/v7.5-v7.6-architecture-plan.md` (Section 8 — Gateway Aggregation)
4. `config/sensors.v2.example.json` — manifest schema reference
5. `scripts/sensor_manifest_lib.py` — validation patterns
6. Latest session log(s)

---

## Exact scope

Define aggregator configuration schema and loader.

1. Create `config/aggregator.example.json` with 2 satellite entries
2. Add `load_aggregator_config()` and `validate_aggregator_config()` to `sensor_manifest_lib.py`
3. Validation: unique satellite IDs, valid HTTP URLs, poll_interval 10-300s
4. Generator conditionally produces aggregator config header when `config/aggregator.json` exists
5. Satellite firmware completely unaffected when aggregator.json absent
6. Update changelog, session log, bump to 7.5.5.0

**No runtime changes.** Configuration schema only.

---

## Post-merge tag

```bash
git tag -a v7.5.5.0 -m "Phase 5 Step 0: Aggregator config schema"
git push origin v7.5.5.0
```
