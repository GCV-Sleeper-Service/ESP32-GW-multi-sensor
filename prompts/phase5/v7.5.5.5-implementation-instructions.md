# v7.5.5.5 — Phase 5 Closure

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.5.4 complete and merged (aggregator Playwright tests pass)
- All root baseline, mixed-category, and aggregator test suites pass (confirm)
- Aggregator device-tested with real satellite (confirm)
- Moving to Phase 5 closure
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase5-implementation-plan.md` (v7.5.5.5 section)
2. `Docs/bugs-and-lessons-learned.md` (add any new Phase 5 entries)
3. `Docs/changelog.md`
4. `Docs/v7.5-v7.6-architecture-plan.md` — Phase 5 status section
5. All session logs from Phase 5 (v7.5.5.0–v7.5.5.4)
6. Latest session log(s)

---

## Exact scope

Documentation, aggregator setup guide, and Phase 5 closure.

### Step-by-step:

1. **Create `Docs/aggregator-setup.md`** — Deployment guide covering:
   - Hardware recommendations (ESP32-S3 preferred, C3 for max 1–2 satellites, RPi for larger)
   - Configuration: creating `config/aggregator.json` from example
   - Network requirements (aggregator must reach all satellite IPs)
   - Building aggregator firmware (vs satellite firmware)
   - Flashing and first boot
   - Monitoring aggregator health
   - Troubleshooting: satellite unreachable, high latency, memory pressure
   - Security considerations (auth, VLAN placement)

2. **Update `Docs/v7.5-v7.6-architecture-plan.md`:**
   - Mark Phase 5 Status: **COMPLETE** ✅
   - Add Phase 5 summary with all step dates and descriptions

3. **Update `Docs/changelog.md`:**
   - v7.5.5.5 entry with "Phase 5 Complete" callout
   - Phase 5 summary table

4. **Update `Docs/bugs-and-lessons-learned.md`:**
   - Add any new bugs discovered during Phase 5
   - Add any new operational lessons (especially around multi-device testing, aggregator-specific patterns)

5. **Create final Phase 5 session log**

6. **Bump version to 7.5.5.5** via `scripts/bump-version.sh`

7. **Regenerate all artifacts**

**Do NOT** begin Phase 6. This is closure only.

---

## Critical rules

1. Use `bash scripts/bump-version.sh 7.5.5.5` for version bump
2. Run full test suite + preflight — must all pass
3. Aggregator-setup.md must be comprehensive enough for a new user to deploy an aggregator from scratch
4. Phase 5 summary in changelog should list each step and its scope

---

## Device testing (for human, after merge)

### Prerequisites — pull, compile, flash

```bash
cd /config/ESP32-GW-multi-sensor
git pull origin main

cat VERSION
# Expected: 7.5.5.5

# Final compile + flash (satellite mode):
rm -f config/aggregator.json
python3 scripts/render_sensor_config.py --write
esphome compile firmware/esp32-c3-multi-sensor.yaml
esphome run firmware/esp32-c3-multi-sensor.yaml
```

### Final verification

```bash
# 1. Verify all API endpoints on satellite:
curl -s http://192.168.120.189/api/manifest | python3 -m json.tool
curl -s http://192.168.120.189/api/v2/live | python3 -m json.tool
curl -s http://192.168.120.189/api/status | python3 -m json.tool

# 2. Open dashboard — verify normal satellite mode, all card types
# 3. Record final heap baseline:
curl -s http://192.168.120.189/api/status | grep free_heap

# 4. Run full test suites:
npx playwright test --project=chromium
FIXTURE_SET=mixed npx playwright test --project=chromium
FIXTURE_SET=aggregator npx playwright test --project=chromium
bash scripts/preflight.sh
```

---

## Post-merge tag

```bash
git pull origin main
git tag -a v7.5.5.5 -m "Phase 5 Complete: Aggregator MVP fully operational"
git push origin v7.5.5.5
```
