# v7.5.5.2 — Phase 5 Step 2

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.5.1 complete (aggregator polling task, caching satellite data)
- Aggregator successfully polls satellite (confirm)
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase5-implementation-plan.md` (v7.5.5.2 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/v7.5-v7.6-architecture-plan.md` (Section 8 — Gateway Aggregation)
4. `dashboard/sensor_history_multi.h` — existing endpoint handler patterns
6. Latest session log(s)

---

## Exact scope

Expose aggregator-specific API endpoints.

1. `GET /api/aggregator/gateways` — list satellites with status, manifest, reachable flag
2. `GET /api/aggregator/live` — unified live values from all satellites
3. `GET /api/aggregator/proxy/{gw_id}/history/{device}/{metric}` — proxied history request
4. 404 for unknown gateway IDs
5. Add conditional preflight checks (only when aggregator.json exists)
6. Update changelog, session log, bump to 7.5.5.2

---

## Post-merge tag

```bash
git tag -a v7.5.5.2 -m "Phase 5 Step 2: Aggregator API endpoints"
git push origin v7.5.5.2
```
