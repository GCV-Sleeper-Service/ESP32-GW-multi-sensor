# v7.6.10.0 — ESPHome Upgrade Verification (Claude Two-Step Prompt)

_Date: 2026-04-22_
_Step: Phase VX Step 0 — ESPHome 2026.4.1 upgrade verification and local component re-patch_

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read:

  1. `prompts/handoff/phaseVX/session-handoff-v7.6.10.0.md` — context and state.
  2. `prompts/phaseVX/v7.6.10.0-agent-prompt-gpt-codex.md` — full implementation spec.

Then read every file in §1 Required Reading.

### Scope discipline

This step is **verification only**. ESPHome 2026.4.1 is already installed. The agent:

1. Re-runs `scripts/patch-esphome-httpd-stack.sh` to refresh the local component override
2. Verifies `--check` passes
3. Clean-builds all 3 existing boards (compile only, no flash)
4. Updates PATCH_INFO.md, VERSION, changelog
5. Runs preflight + Playwright

**There are NO firmware logic changes.** No handler modifications, no dashboard changes, no partition table changes, no new board profiles.

### Critical invariants

- Stack value in override: exactly 16384 (not conditional, not changed)
- No board profile changes
- No `firmware/core/*.h` changes
- No dashboard module changes
- `provision.sh satellite` is the final state before push

---

⛔ CHECKPOINT A (after patch re-run):
  `bash scripts/patch-esphome-httpd-stack.sh --check` — expected: exit 0

⛔ CHECKPOINT B (after clean builds):
  All 3 boards compile. Record binary sizes for each.

⛔ PRE-PR GATE:
  Preflight + Playwright all green.
  VERSION = 7.6.10.0
  `provision.sh satellite` run last.

---

⚠️ MANDATORY DEVICE TEST AFTER MERGE — operator executes:
  Flash all 3 boards. Run stress-test-httpd-stack.sh on each.
  Watermarks must be ≥ 10,000 bytes (no regression from v7.6.9.5 baselines).

---

## Step 2 — Review Session

After agent creates PR, review checking:

### Scope discipline
  • Local component override refreshed from ESPHome 2026.4.1 upstream?
  • Stack value still exactly 16384?
  • `--check` passes in CI or agent output?
  • No firmware handler changes (`firmware/core/` untouched)?
  • No dashboard changes?
  • No board profile changes?
  • No partition table changes?

### Compilation verification
  • C3 compiles successfully? Binary size recorded?
  • WROOM compiles successfully? Binary size recorded?
  • S3 compiles successfully? Binary size recorded?
  • Any binary size increase > 20% from v7.6.9.5? (Flag if so — investigate)

### Documentation
  • PATCH_INFO.md updated with ESPHome 2026.4.1 / ESP-IDF 5.5.4?
  • Changelog entry present and accurate?
  • VERSION = 7.6.10.0?

### PR hygiene
  • Session log produced?
  • Instruction Compliance Output table in PR description?
  • `provision.sh satellite` was the last command (CI-safe state)?

Post-merge deliverables:
  - Consolidated audit: `prompts/phaseVX/v7.6.10.0-PRNN-consolidated-audit-and-lessons.md`
  - Operator: flash + stress test all 3 boards
  - Tag: `git tag -a v7.6.10.0 -m "Phase VX: ESPHome 2026.4.1 upgrade verification" && git push origin v7.6.10.0`

---

## External Reviewer Supplement

> **v7.6.10.0 focus: ESPHome upgrade verification, zero logic changes.**
>
> ESPHome was upgraded from 2026.2.1 to 2026.4.1. This PR re-applies the local component override (16 KB httpd stack + HTTP_DELETE handler) and verifies all 3 existing boards still compile.
>
> **Blocking issues:**
> 1. `patch-esphome-httpd-stack.sh --check` fails — patch did not apply cleanly
> 2. Stack value changed from 16384
> 3. Any existing board fails to compile
> 4. Any firmware handler changes (scope violation)
> 5. Missing VERSION bump or changelog
>
> **Key check:** `grep 'config.stack_size = 16384' firmware/local_components/web_server_idf/web_server_idf.cpp` must match.

---

_End of v7.6.10.0 two-step prompt._
