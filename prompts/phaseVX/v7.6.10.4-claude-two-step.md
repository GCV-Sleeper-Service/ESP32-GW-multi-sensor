# v7.6.10.4 — Dashboard Authentication Refactor (Claude Two-Step Prompt)

_Date: 2026-05-05_
_Step: Phase VX Step 4 — Unified dashboard authentication, eliminate browser auth dialogs_
_Status: 🔜 Next_

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read:

  1. `prompts/handoff/phaseVX/session-handoff-v7.6.10.4.md` — context and state.
  2. `prompts/phaseVX/v7.6.10.4-agent-prompt-gpt-codex.md` — full implementation spec.

Then read every file in §1 Required Reading.

### Scope discipline

This step modifies **dashboard JavaScript only**. No firmware changes. No C++ changes. No partition table changes. No board profile changes.

The agent:

1. Creates `dashboard/core/auth.js` — new core module
2. Modifies 9 existing dashboard files to replace `fetch()` with `authFetch()`
3. Extends `dashboard/components/auth-modal/index.js` to support the new auth flow
4. Runs the full dashboard build pipeline
5. Runs preflight + Playwright
6. Updates VERSION, changelog

**There are NO firmware logic changes.** No `web-handler.h` modifications. No new endpoints. No partition table changes. No board profile changes.

### Critical invariants

- No files under `firmware/core/` are modified
- No files under `firmware/boards/` are modified
- No files under `partitions/` are modified
- POST body handling: `application/x-www-form-urlencoded` with `body: 'a=1'` — no JSON POST
- No localStorage/sessionStorage usage (blocked in artifacts, inappropriate for embedded credentials)
- `provision.sh satellite` is the final state before push

### Regenerated files note

Dashboard changes trigger pipeline regeneration of `dashboard.js`, `dashboard.html`, and
`dashboard.h`. This is expected behavior per Critical Rule 37. The agent prompt's "Files
modified" section lists only directly modified files. See LESSON-OPS-129.

---

⛔ CHECKPOINT A (after auth.js creation):
  `grep -c 'authFetch' dashboard/core/auth.js` — expected: ≥ 1
  `grep -c '_authHeader' dashboard/core/auth.js` — expected: ≥ 1

⛔ CHECKPOINT B (after fetch→authFetch conversion):
  `grep -rn "credentials.*same-origin" dashboard/core/ dashboard/components/` — expected: 0 results
  `grep -rn "authFetch" dashboard/core/ dashboard/components/ --include="*.js" | wc -l` — expected: ≥ 10

⛔ CHECKPOINT C (after pipeline run):
  `bash scripts/bundle-dashboard.sh --write` — exit 0
  `python3 scripts/render_sensor_config.py --write` — exit 0
  `bash scripts/build-dashboard.sh --write` — exit 0
  `bash scripts/minify-dashboard.sh` — exit 0
  `bash scripts/generate-header.sh` — exit 0
  `python3 scripts/render_sensor_config.py --check` — exit 0

⛔ PRE-PR GATE:
  `bash scripts/preflight.sh` — all green
  `npx playwright test` — all 402 tests pass
  VERSION = 7.6.10.4
  `provision.sh satellite` run last.

---

## Step 2 — Perplexity Three-Turn Review

### Turn 1 (to Perplexity):

Review PR #NNN in GCV-Sleeper-Service/ESP32-GW-multi-sensor.

Context: This PR implements a dashboard authentication refactor (v7.6.10.4) — replacing browser-managed HTTP Basic Auth credential caching with application-level credential management via a new `auth.js` core module.

Focus areas:
1. Does `authFetch()` correctly inject the `Authorization` header in all fetch calls?
2. Are there any fetch calls missed (still using plain `fetch()` for auth-gated endpoints)?
3. Is the 401 re-auth flow correct (clear credentials → show dialog → resume)?
4. Is credential storage appropriately session-scoped (no localStorage/sessionStorage)?
5. Does the auth probe on boot correctly distinguish "no auth needed" from "auth required"?
6. POST body handling: all POST calls use `application/x-www-form-urlencoded`, not JSON?

### Turn 2 (after Perplexity responds):

Please also check:
1. The `auth-modal` component integration — does the existing dialog's submit flow correctly set `_authHeader`?
2. Error handling: what happens if the auth dialog's verify fetch fails (network error vs wrong credentials)?
3. SSE connection: does the EventSource for SSE need auth headers? (Note: EventSource API does not support custom headers — verify the approach)
4. The pipeline was run correctly (check that `dashboard.js` and `dashboard.html` are regenerated)

### Turn 3 (after Perplexity responds):

Final verification:
1. Confirm no `credentials: 'same-origin'` remains in any dashboard file
2. Confirm Playwright tests pass (check CI output)
3. List any concerns about the auth flow that should be documented

---

## Post-Merge Deliverables

After merge, Claude produces:

1. **Session log reference** — note PR number, test results, review findings
2. **Lessons learned** — any new LESSON-OPS entries (e.g., SSE auth limitations)
3. **POST-STEP-EDIT-TRACKER update** — check off v7.6.10.4 items
4. **Phase VX closure assessment** — is VX ready to close?

---

_End of two-step prompt._
