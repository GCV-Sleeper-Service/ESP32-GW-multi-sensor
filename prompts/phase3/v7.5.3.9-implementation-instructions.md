# v7.5.3.9 — Phase 3 Closure

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.3.8 complete and merged (SensorSlot removed, SensorEntity is sole model)
- Device test: ALL checks PASS (confirm before starting)
- Moving to v7.5.3.9 — Phase 3 Closure
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase3-implementation-plan.md` (v7.5.3.9 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/v7.5-v7.6-architecture-plan.md`
4. Latest session log(s)

---

## Exact scope

Phase 3 closure — testing, validation, documentation.

1. **Add Phase 3 closure Playwright tests (Group 15):**
   - `/api/v2/live` returns valid JSON with all device IDs from manifest
   - `/api/v2/live` returns metric keys matching manifest definitions
   - `/api/v2/history/{device}/{metric}` returns CSV data
   - Legacy `/history/{id}/temp` still works
   - Legacy `/sensors.json` still works
   - Dashboard renders identically with new endpoints
   - All existing tests pass (regression gate)

2. **Add mock routes** to `tests/mock-server/server.js`:
   - `GET /api/v2/live` — return fixture live data
   - `GET /api/v2/history/:device/:metric` — return fixture CSV

3. **Update documentation:**
   - `Docs/changelog.md` — v7.5.3.9 entry with "Phase 3 Complete" callout
   - `Docs/v7.5-v7.6-architecture-plan.md` — Phase 3 Status: COMPLETE
   - Create session log
   - Update bugs-and-lessons-learned if new bugs found

4. **Bump version** to 7.5.3.9
5. **Regenerate** all artifacts

**Do NOT:** Begin Phase 4. This is closure only.

---

## Post-merge tag

```bash
git tag -a v7.5.3.9 -m "Phase 3 Complete: SensorEntity model fully operational"
git push origin v7.5.3.9
```
