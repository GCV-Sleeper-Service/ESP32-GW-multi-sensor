# Phase X — PR Audit: v7.6.5.0 (PR #132)
_Date: 2026-04-06_  
_Level: Level 1 — Module Split_  
_PR: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor/pull/132_  
_Result: MERGED — PASS_

---

## Section 1 — Internal Audit (Architectural Advisor)

1. **Prompt delivery:** Delivered the required 21-module split and bundler. Two deviations resolved post-review: (a) `bundle-dashboard.sh` initially accepted unknown flags as `--write` (fixed to strict arg validation) and (b) `bump-version.sh` side-effect updated `dashboard.html`/`.h` despite the Level 1 “HTML unchanged” rule (reverted and preflight aligned to HTML-sourced version). Post-closure patches also updated the v7.6.5.1 prompt/hand-off to reflect the corrected bundle-first pipeline order.
2. **Codebase state vs. assumptions:** Assumed `dashboard.html` would remain untouched, but the bump-version workflow still edited it; detected during review and reverted. Otherwise state matched instructions (modules present, identity gate intact).
3. **Autonomous decisions:** Kept initial permissive CLI parsing for the bundler (later corrected); re-extracted modules after generator version change to preserve identity; chose to update preflight to read the HTML version to avoid drift when HTML is intentionally frozen.
4. **New lessons / candidate Critical Rules:**  
   - **Candidate LESSON-OPS-098:** Bundle scripts must validate CLI flags strictly; lenient defaults can silently overwrite generated artifacts on typos.  
   - **Candidate LESSON-OPS-099:** For Level 1 (HTML frozen), any version sourcing must be driven from the frozen HTML, and bump-version side effects on HTML must be reverted or blocked; preflight should key off the frozen artifact to catch drifts.
5. **Carry-forward context for v7.6.5.1:** Identity gate remains the primary acceptance check; HTML stays unchanged at Level 1; bundler now requires explicit `--write/--check`; regeneration/pipeline prompts must place `bundle-dashboard.sh --write` before generator steps; external review items on escaping/device-count precedence are pre-existing and deferred.

---

## Section 2 — Level 1 Supplement Answers

- **Identity gate:** Pass — SHA-256 before = after bundle.  
- **Contiguous slices:** Yes — modules are direct file-order slices; no function reordering.  
- **Behavioral changes introduced:** None; final state matches pre-split behavior, HTML reverted.  
- **`bundle-dashboard.sh --check` on clean tree:** Pass.

---

## Section 3 — External Review Summary

- **Review 4060169399 (Copilot):** Flagged bundler arg permissiveness (warranted, fixed); other findings were pre-existing (escaping gaps, device/sensor count precedence, safeJsonResponse usage).  
- **Review 4060179861 (Gemini):** Flagged the same pre-existing issues; all classified “pre-existing, out of scope for structural split.” Logged in the pre-existing issues table below.  
- **Issue comments 4190129083 (Codex) & 4190139391 (GPT):** Both warranted; fixes applied in post-review commit (bundler strict mode, HTML/header revert, preflight version sourcing).

**Pre-existing issues surfaced by extraction**

| Issue | Module | Severity | Action |
|-------|--------|----------|--------|
| `FILE_FALLBACK_HOST` hardcoded to `http://192.168.120.189` | `01-config-state.js` | Medium | Defer to follow-up PR (pre-existing; identity gate prohibits change here) |
| `btoa()` without Unicode guard in `postManagementAction` | `12-management.js` | Low | Defer to follow-up PR (pre-existing) |
| `pollAll(livePaths, batchSize=4)` contradicts BUG-043 sequential fix | `18-transport.js` | Medium | Defer to follow-up PR (pre-existing; BUG-043-cont fix only covers fetchDeviceHistory) |
| `device.name` interpolated into innerHTML without `escHtml()` | `14-cards.js` | Medium | Defer to follow-up PR (pre-existing; escHtml exists but not applied uniformly) |

---

## Section 4 — Issues Introduced by This PR (new)

- `bundle-dashboard.sh` treated unknown flags as `--write` → **Fixed** in post-review commit (strict arg parsing, usage on unknown).  
- `dashboard.html` / `dashboard.h` changed by bump-version side effect despite Level 1 freeze → **Fixed** in post-review commit (HTML/header reverted; preflight reads HTML version string).

---

## Section 5 — Follow-up Work Items

1. Clean up `FILE_FALLBACK_HOST` hardcoding; make configurable or remove fallback. Modules: `01-config-state.js`. Severity: Medium. Target: v7.6.5.x follow-up.  
2. Add Unicode-safe encoding for `postManagementAction` (`btoa` guard). Modules: `12-management.js`. Severity: Low. Target: v7.6.5.x follow-up.  
3. Make live polling fully sequential (`pollAll` batchSize=1) to complete BUG-043 remediation. Modules: `18-transport.js`. Severity: Medium. Target: v7.6.5.x follow-up.  
4. Apply `escHtml`/`escAttr` uniformly in cards (device/sensor names). Modules: `14-cards.js`. Severity: Medium. Target: v7.6.5.x follow-up hardening.  
5. Ensure variant fixtures under `tests/fixtures/variants/` stay version-synced in future bumps. Modules: fixtures tooling. Severity: Low. Target: v7.6.5.x follow-up.  
6. Keep regeneration prompts/hand-offs aligned with the bundle-first pipeline; re-verify before starting v7.6.5.1 execution. Modules: prompts. Severity: Low. Target: v7.6.5.1 readiness.

---

## Section 6 — Pipeline Notes Captured

- `render_sensor_config.py` performs two modifications: sentinel marker injection and `App.version` regex replacement.  
- After any version bump or generator run, modules must be re-extracted from the post-pipeline `dashboard.js` to keep source slices aligned.  
- Canonical Level 1 workflow: extract modules → bundle → version bump (generator) → re-extract modules → bundle → `bundle-dashboard.sh --check`; regeneration prompts should run `bundle-dashboard.sh --write` before generator steps to avoid wiping injected markers.

---

_End of consolidated audit._
