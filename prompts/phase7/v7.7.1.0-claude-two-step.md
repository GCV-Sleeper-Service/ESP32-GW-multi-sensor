# v7.7.1.0 — Health-Check Telemetry Task (Claude Two-Step Prompt)

_Date: 2026-05-07_
_Step: Phase 7 Step 0b — first implementation step_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phase7/session-handoff-v7.7.1.0.md` — session context.
  2. `prompts/phase7/v7.7.1.0-agent-prompt-gpt-codex.md` — full implementation spec.
  3. Read every file in that prompt's §1 Required Reading, paying attention to:
   - `firmware/core/deferred-management.h` — `xTaskCreate` pattern (lines ~9, ~29) for stack size and priority conventions
   - `firmware/core/web-handler.h` — existing `nvs_get_stats` usage (line ~1265) and `uxTaskGetStackHighWaterMark` (line ~1384) for API patterns
   - `scripts/assemble-sensor-history.sh` — MODULES array order (lines ~17–26)

Then implement in this order:

  1. Create `firmware/core/health-check.h` per §6 Task Group 1
  2. Verify file content per ⛔ CHECKPOINT A

---
⛔ CHECKPOINT A (after creating health-check.h):
Run:
  `grep -c 'xTaskCreate.*health_check' firmware/core/health-check.h` → expected: 1
  `grep -c 'nvs_get_stats' firmware/core/health-check.h` → expected: ≥1
  `grep -c 'esp_get_free_internal_heap_size' firmware/core/health-check.h` → expected: ≥1
  `ls firmware/core/*.h | wc -l` → expected: 9
If ANY check fails: STOP.
---

  3. Update `scripts/assemble-sensor-history.sh` — insert health-check.h between nvs-persistence.h and deferred-management.h
  4. Run assembly: `bash scripts/assemble-sensor-history.sh --write && bash scripts/assemble-sensor-history.sh --check`

---
⛔ CHECKPOINT B (after assembly update):
Run:
  `grep -c 'health-check.h' scripts/assemble-sensor-history.sh` → expected: 1
  `bash scripts/assemble-sensor-history.sh --check` → expected: PASS
If ANY check fails: STOP.
---

  5. Add health-check task start to YAML on_boot at priority 600
  6. Add preflight check for health-check.h existence
  7. Version bump, full pipeline, preflight, Playwright
  8. Changelog, CURRENT-STATE.md, session log

---
⛔ PRE-PR GATE:
  `git diff --name-only` → only expected files (see §8 in agent prompt)
  `bash scripts/preflight.sh` → PASS
  `bash scripts/assemble-sensor-history.sh --check` → PASS
  All 5 Playwright fixture sets green
---

Do NOT modify any existing firmware fragments. Do NOT create HTTP endpoints.
Do NOT edit `dashboard/sensor_history_multi.h` directly (Rule 58).

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read `prompts/handoff/phase7/session-handoff-v7.7.1.0.md` for context.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • `health-check.h` uses `esp_get_free_internal_heap_size()` (not just `esp_get_free_heap_size()`) — Rule 24?
  • `heap_caps_get_minimum_free_size(MALLOC_CAP_INTERNAL)` for min_free (not `esp_get_minimum_free_heap_size()` alone)?
  • `uxTaskGetStackHighWaterMark` result multiplied by `sizeof(StackType_t)` for bytes?
  • `nvs_get_stats` called with `HISTORY_PARTITION_LABEL` (not hardcoded string)?
  • `xTaskCreate` stack is 4096, priority is 1?
  • Task name ≤ 16 characters?
  • 30-second initial delay before first report?
  • `health-check.h` is between `nvs-persistence.h` and `deferred-management.h` in MODULES?
  • No NVS write operations in the health-check task?
  • No HTTP endpoints created?
  • No existing fragments modified?
  • `assemble-sensor-history.sh --check` passes?
  • `start_health_check_task_()` called in YAML `on_boot` at priority 600?
  • Preflight check added for health-check.h?
  • CURRENT-STATE.md updated (version, "What Just Shipped", unimplemented recommendations)?
  • Critical Rules 65-67 added to `prompts/prompt-index-and-workflow.md`?
  • Phase 7 step tracking issue for v7.7.1.0 referenced in PR body?
  • All Playwright tests pass across all fixture sets?
  • Session log created with required sections (Rule 63)?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phase7/v7.7.1.0-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.7.1.1.md` and `v7.7.1.1-agent-prompt-gpt-codex.md`

Operator device testing (post-merge — results go in consolidated audit):
  - Flash C3, wait 90s, check serial logs for `HEALTH:` lines
  - Record heap_free, min_free, httpd_stack_wm, nvs_used values as v7.7.1.0 baseline
  - Verify health-check doesn't measurably reduce free_heap (task stack is only 4096 B)

Tag: `git tag -a v7.7.1.0 -m "Phase 7: Health-check telemetry task" && git push origin v7.7.1.0`

---

## External Reviewer Workflow

After the agent opens the PR, send the universal reviewer prompt with Phase 7 focus areas to external reviewers (Codex, GPT, Copilot). Key review focus:

1. Does the health-check task correctly separate internal vs total heap reporting?
2. Is the `uxTaskGetStackHighWaterMark` → bytes conversion correct (multiply by `sizeof(StackType_t)`)?
3. Does the assembly order respect fragment dependency rules?
4. Is there any risk of the 4096-byte task stack being insufficient?

---

_End of v7.7.1.0 Claude two-step prompt._
