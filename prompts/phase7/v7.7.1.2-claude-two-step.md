# v7.7.1.2 — Per-Device Structs, Key Scheme, Hash Function — Claude Two-Step

_Date: 2026-05-08_
_Risk: LOW_

---

## Step 1 — Agent Execution

Copy the full content of `prompts/phase7/v7.7.1.2-agent-prompt-gpt-codex.md` to the coding agent.

Setup instructions for operator:
```
1) Sync latest main and branch:
   git -C /root/config/ESP32-GW-multi-sensor fetch origin main
   git -C /root/config/ESP32-GW-multi-sensor checkout -b feature/v7.7.1.2-per-device-structs origin/main
2) Create empty bootstrap commit, push branch, open DRAFT PR to main
3) Post PR URL immediately
4) Read required files in order, run pre-implementation gates, start edits
5) Commit only to that branch, never to main
6) When done: gh pr ready <PR#> --repo GCV-Sleeper-Service/ESP32-GW-multi-sensor
7) Update PR description (use PR225-226 as reference)
```

---

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read `prompts/handoff/phase7/session-handoff-v7.7.1.2.md` for context.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • `DeviceHistoryMeta` struct matches architecture doc §5.1? (36 bytes, all fields present)
  • `DeviceSegment` struct matches architecture doc §5.2? (226 bytes, `MAX_PERSIST_METRICS=6`, `PERSIST_POINTS_PER_SEGMENT=4`)
  • `DeviceSegmentHeader` has `magic`, `version`, `metric_count`, `points_per_segment`, `saved_at_epoch`, `first_epoch`, `last_epoch`?
  • `EventLog` matches Phase V capacity study §6? (20 entries, 168 bytes, ring buffer with `add`/`at_logical`/`export_entries`/`load_from`)
  • `EventEntry` has `epoch` (4B), `state` (1B), `_pad[3]` (3B) = 8 bytes?
  • `device_id_hash_()` uses FNV-1a constants: offset `0x811c9dc5`, prime `0x01000193`?
  • `make_device_meta_key_()` produces `dm_HHHHHHHH` format (≤11 chars)?
  • `make_device_segment_key_()` produces `ds_HHHHHHHH_NNN` format (≤15 chars)?
  • Old structs (`HistEntry`, `HistoryBuffer`, `SegmentSnapshot`, `HistoryMeta`) untouched?
  • No runtime wiring — no calls to the new functions from any handler or lambda?
  • No new fragments added (count stays at 9)?
  • No changes to web-handler.h?
  • `esphome compile` succeeded?
  • CURRENT-STATE.md updated (mandatory — version, What Just Shipped)?
  • Session log created and committed to branch (Rule 63)?
  • Consolidated audit created and committed to branch (E-1)?
  • Changelog entry added?

**In-PR deliverables checklist (all must be committed BEFORE marking ready):**
  - [ ] Code changes + compile verification
  - [ ] CURRENT-STATE.md update
  - [ ] Changelog entry
  - [ ] Session log
  - [ ] Consolidated audit

**Post-merge (tag only):**
```
git tag -a v7.7.1.2 -m "Phase 7: Per-device structs, key scheme, hash function" && git push origin v7.7.1.2
```

---

## External Reviewer Workflow

This is a LOW-RISK step. Send to Copilot + Codex inline, then one external reviewer. Focus areas:

1. **Struct layout correctness:** Do the struct sizes match what the architecture doc specifies? Any alignment/padding issues?
2. **FNV-1a implementation:** Is the hash function correct? Does it match the standard FNV-1a algorithm?
3. **Key length safety:** Can any `snprintf` call produce a key longer than 15 characters?
4. **EventLog ring buffer:** Does the ring buffer logic match `HistoryBuffer`'s proven pattern? Any off-by-one risks?
5. **No runtime side effects:** Confirm no code path calls any of the new functions at runtime.

---

_End of v7.7.1.2 Claude two-step prompt._
