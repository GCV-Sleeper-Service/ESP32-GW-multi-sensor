# v7.5.4.4 — Phase 4 Closure

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.4.3 complete and merged (mixed-category Playwright tests pass)
- All root baseline + mixed-category tests pass (confirm)
- Device tested: dashboard shows environmental + network cards, stable (confirm)
- Moving to Phase 4 closure
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase4-implementation-plan.md` (v7.5.4.4 section)
2. `Docs/bugs-and-lessons-learned.md` (add any new entries from Phase 4 work)
3. `Docs/changelog.md`
4. `Docs/v7.5-v7.6-architecture-plan.md` — Phase 4 status section
5. All session logs from Phase 4 (v7.5.4.0–v7.5.4.3)
6. Latest session log(s)

---

## Exact scope

Phase 4 closure — final validation, documentation, phase close.

### Step-by-step:

1. **Verify ALL tests pass:**
   - Root baseline: `npx playwright test --project=chromium`
   - Mixed-category: `FIXTURE_SET=mixed npx playwright test --project=chromium`
   - Preflight: `bash scripts/preflight.sh`

2. **Update `Docs/v7.5-v7.6-architecture-plan.md`:**
   - Mark Phase 4 Status: **COMPLETE** ✅
   - Add Phase 4 summary (similar to Phase 2/3 summary format)
   - List all v7.5.4.x steps with completion dates

3. **Update `Docs/changelog.md`:**
   - v7.5.4.4 entry with "Phase 4 Complete" callout
   - Phase 4 summary table (similar to Phase 3 summary)

4. **Update `Docs/bugs-and-lessons-learned.md`:**
   - Add any new bugs discovered during Phase 4
   - Add any new operational lessons

5. **Create final Phase 4 session log** in `Docs/session-log-<date>-v7.5.4.4.md`

6. **Bump version to 7.5.4.4** via `scripts/bump-version.sh`

7. **Regenerate all artifacts** (generator, header, preflight)

**Do NOT** begin Phase 5. This is closure only.

---

## Critical rules

1. Use `bash scripts/bump-version.sh 7.5.4.4` for version bump
2. Run full test suite + preflight — must all pass
3. Phase 4 summary in changelog should list each step and its scope
4. Architecture plan status update must match the Phase 2/3 pattern

---

## Device testing (for human, after merge)

### Prerequisites — pull, compile, flash

```bash
cd /config/ESP32-GW-multi-sensor
git pull origin main

cat VERSION
# Expected: 7.5.4.4

# Final compile + flash:
esphome compile firmware/esp32-c3-multi-sensor.yaml
esphome run firmware/esp32-c3-multi-sensor.yaml
```

### Final verification

```bash
# 1. Verify all API endpoints:
curl -s http://192.168.120.189/api/manifest | python3 -m json.tool
curl -s http://192.168.120.189/api/v2/live | python3 -m json.tool
curl -s http://192.168.120.189/api/status | python3 -m json.tool
curl -s http://192.168.120.189/sensors.json | python3 -m json.tool

# 2. Open dashboard — verify both card types, stable operation
# 3. Record final heap baseline for Phase 5 comparison:
curl -s http://192.168.120.189/api/status | grep free_heap
```

---

## Post-merge tag

```bash
git pull origin main
git tag -a v7.5.4.4 -m "Phase 4 Complete: First non-climate sensor (ping probe) fully operational"
git push origin v7.5.4.4
```
