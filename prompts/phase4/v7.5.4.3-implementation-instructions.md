# v7.5.4.3 — Phase 4 Step 3

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.4.2 complete and merged (network card renders on dashboard)
- Device test: dashboard shows both environmental and network cards (confirm)
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase4-implementation-plan.md` (v7.5.4.3 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/changelog.md`
4. `tests/browser/dashboard.spec.js` — existing test groups
5. `tests/mock-server/server.js` — existing mock routes
6. `tests/fixtures/` — existing fixture structure
7. Latest session log(s)

---

## Exact scope

Create mixed-category test fixtures and Playwright tests.

1. Create `tests/fixtures/variants/mixed/` — 2 ThermoPro + 1 ping probe
2. Extend `generate-fixtures.js` for mixed-category variant
3. Extend `server.js` to serve mixed-category data including ping history
4. Add Group 16 tests: mixed-category rendering, correct card dispatch, chart rendering for both types
5. Update changelog, session log, bump to 7.5.4.3

---

## Post-merge tag

```bash
git tag -a v7.5.4.3 -m "Phase 4 Step 3: Mixed-category test fixtures"
git push origin v7.5.4.3
```
