# v7.5.4.0 — Phase 4 Step 0

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- Phase 3 complete (v7.5.3.9 merged)
- SensorEntity is the sole C++ model
- Moving to Phase 4: First non-climate sensor category (ping probe)
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase4-implementation-plan.md` (v7.5.4.0 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/changelog.md`
4. `Docs/v7.5-v7.6-architecture-plan.md` (Sections 6.4, 7.2, 10.2)
5. `config/sensors.v2.example.json` — reference for mixed-category manifest
6. `scripts/render_sensor_config.py` and `scripts/sensor_manifest_lib.py`
7. Latest session log(s)

---

## Exact scope

Add ping probe device to manifest and generator. No firmware adapter yet.

1. Upgrade `config/sensors.json` to v2 schema with `wan_ping` device (category: network, adapter: icmp_ping)
2. Extend `sensor_manifest_lib.py` validation: `mac` required only for BLE devices; `source.target` required for ping
3. Extend `render_sensor_config.py`: generate `metrics_ping[]` array, `SensorEntity` for ping device (RAM-only history)
4. Regenerate `sensor_history_multi.h` with 4th SensorEntity (3 ThermoPro + 1 ping)
5. Regenerate `gateway_manifest.h` with wan_ping device
6. Update test fixtures for mixed-category
7. Update changelog, session log, bump to 7.5.4.0

**Critical:** Ping device exists in data model but produces no data. All environmental behavior unchanged.

---

## Post-merge tag

```bash
git tag -a v7.5.4.0 -m "Phase 4 Step 0: Add ping device to manifest"
git push origin v7.5.4.0
```
