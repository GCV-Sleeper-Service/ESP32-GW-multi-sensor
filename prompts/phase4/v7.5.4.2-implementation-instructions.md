# v7.5.4.2 — Phase 4 Step 2

_Implementation instructions for the AI assistant_

---

## Context

Repo: https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

**Current status:**
- v7.5.4.1 complete and merged (ICMP ping adapter running)
- Device test: /api/v2/live shows ping_ms and success_pct values (confirm)
- Current date: <INSERT_DATE>

---

## Required reading

1. `Docs/phase4-implementation-plan.md` (v7.5.4.2 section)
2. `Docs/bugs-and-lessons-learned.md`
3. `Docs/changelog.md`
4. `Docs/v7.5-v7.6-architecture-plan.md` (Section 7.2 — Card renderer registry)
5. `dashboard/dashboard.js` — understand CARD_RENDERERS, buildEnvironmentalCard, handleState
6. `dashboard/dashboard.html` — CSS structure
7. Latest session log(s)

---

## Exact scope

Add `CARD_RENDERERS.network` to dashboard.

1. Implement `buildNetworkCard()` function — shows latency, success rate, target host
2. Add `METRIC_FORMATTERS.ping_latency` and `METRIC_FORMATTERS.success_rate`
3. Update `handleState()` to recognize non-environmental state updates
4. Add network card CSS styles if needed
5. Mirror to dashboard.html, regenerate dashboard.h
6. Update changelog, session log, bump to 7.5.4.2

**Critical:** Environmental cards must be PIXEL-IDENTICAL to pre-Phase-4. Test carefully.

---

## Post-merge tag

```bash
git tag -a v7.5.4.2 -m "Phase 4 Step 2: Add network card renderer"
git push origin v7.5.4.2
```
