# v7.6.7.1 — Import Crash Fix — Deferred Task Pattern (Claude Two-Step Prompt)

_Date: 2026-04-12_
_Step: V1-D_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.7.1.md` — session context.
  2. `prompts/phaseV/v7.6.7.1-agent-prompt-gpt-codex.md` — full spec.
  3. §1 Required Reading from that prompt, paying attention to:
   - `firmware/core/web-handler.h` — `handle_import_begin_()` at line ~779, `build_import_epoch_map_()` at line ~791 (synchronous, violates Rule 40), `beginResponseStream` at line ~817 (Rule 8 violation)

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement:

  1. Add `static volatile bool s_import_ready = false;` near other `s_import_*` variables
  2. Add `import_epoch_map_task_()` function (calls `build_import_epoch_map_()`, sets `s_import_ready = true`)
  3. Replace synchronous `build_import_epoch_map_()` call with `xTaskCreate(import_epoch_map_task_, "imp_epoch", 8192, ...)`
  4. Respond immediately with `{"ok":true,"status":"queued"}` using `beginResponse()` (NOT `beginResponseStream`)

---
⛔ CHECKPOINT A (after deferred task):
Run:
  grep -c "xTaskCreate.*imp_epoch" firmware/core/web-handler.h — expected: ≥ 1
  grep -c "s_import_ready" firmware/core/web-handler.h — expected: ≥ 3
  grep "beginResponseStream" firmware/core/web-handler.h | grep -c "817" — expected: 0 (fixed)
If ANY fails: STOP.
---

  5. Add `/api/import/status` endpoint to `canHandle()` and `handleRequest()`
     Auth: NOT REQUIRED — dashboard polls this; returns only ready boolean
  6. Gate `/api/import/d/` and `/api/import/w/` on `s_import_ready` (return 409 if not ready)
  7. Reassemble, version bump, pipeline, Playwright

---
⛔ PRE-PR GATE:
  git diff --name-only — only web-handler.h fragment + generated artifacts + docs
  bash scripts/preflight.sh — all pass
  bash scripts/assemble-sensor-history.sh --check — PASS
  All Playwright fixture sets green
---

Do NOT modify other fragments. Do NOT add auth guards. Do NOT modify dashboard JS.

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.7.1.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • CRITICAL: `build_import_epoch_map_()` runs in `xTaskCreate` (NOT on httpd task) — Rule 40?
  • Task stack is ≥ 8192 bytes?
  • `s_import_ready` uses `volatile` qualifier?
  • HTTP response sent BEFORE task starts (deferred pattern correct)?
  • `beginResponseStream` at line ~817 replaced with `beginResponse()` — Rule 8?
  • `/api/import/status` has NO auth guard (intentionally public)?
  • `/api/import/status` returns `{"ready":true/false}` — no sensitive data exposed?
  • Import data endpoints (`/api/import/d/`, `/api/import/w/`) gate on `s_import_ready`?
  • Gate returns 409 with clear message when not ready?
  • Both single-sensor and multi-sensor import paths use deferred task?
  • No changes to other fragments?
  • All Playwright tests pass?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.7.1-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.7.2.md` and `v7.6.7.2-agent-prompt-gpt-codex.md`

Operator device testing (CRITICAL for this step — test on C3 hardware):
  - `POST /api/import/begin` returns immediately — NO crash, NO WDT reset
  - `GET /api/import/status` returns `{ready:false}` then `{ready:true}`
  - Full import sequence completes without watchdog reset
  - Free heap does not drop below 55 KB during import task

Tag: `git tag -a v7.6.7.1 -m "Phase V V1-D: Import crash fix (deferred task)" && git push origin v7.6.7.1`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers (Codex, GPT, Copilot). Append the step-specific focus areas from the same template's lookup table for v7.6.7.1.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.7.1 Claude two-step prompt._
