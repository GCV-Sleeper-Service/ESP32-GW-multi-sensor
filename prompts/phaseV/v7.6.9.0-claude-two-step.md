# v7.6.9.0 — Dashboard Device Card Cleanup (#143 + #144 + #136 + #138) (Claude Two-Step Prompt)

_Date: 2026-04-12_
_Step: V3-A_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.0.md` — session context.
  2. `prompts/phaseV/v7.6.9.0-agent-prompt-gpt-codex.md` — full spec.
  3. §1 Required Reading, focusing on `DEVICE_INFO_MAP`, manifest population, device card template.

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement:

  1. #144: Replace MAC row with Device Name + Firmware Version rows in template
  2. Update `DEVICE_INFO_MAP` in `status-snapshot.js`
  3. Populate new fields from manifest in `manifest.js`
  4. #136: Add flash/SRAM text_sensors to YAML + DEVICE_INFO_MAP
  5. #138: Add PSRAM sensors (S3: runtime values, C3: "None")
  6. Dashboard rebuild pipeline (bundle → build → minify → header)
  7. Version bump, preflight, Playwright

---
⛔ PRE-PR GATE: preflight + Playwright
---

Do NOT edit dashboard.js or dashboard.html directly (Rule 47).

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.9.0.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • `DEVICE_INFO_MAP` changes are in `dashboard/core/status-snapshot.js` (NOT in components/ subdirectory)?
  • Manifest population is in `dashboard/core/manifest.js`?
  • New text_sensor entities present in YAML for flash/SRAM/PSRAM?
  • Dashboard rebuild pipeline run (4 commands)?
  • No direct edits to generated artifacts — Rules 47, 48?
  • S3 shows PSRAM values, C3 shows 'None'?
  • No regression in env/ping/system card rendering?
  • All Playwright tests pass?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.9.0-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.9.1.md` and `v7.6.9.1-agent-prompt-gpt-codex.md`

Operator device testing: flash C3 and S3, verify device card shows correct info for each board.

Tag: `git tag -a v7.6.9.0 -m "Phase V V3-A: Device card cleanup" && git push origin v7.6.9.0`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers (Codex, GPT, Copilot). Append the step-specific focus areas from the same template's lookup table for v7.6.9.0.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.9.0 Claude two-step prompt._
