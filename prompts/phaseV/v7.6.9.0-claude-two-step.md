# v7.6.9.0 — Dashboard Device Card Cleanup (#143 + #144 + #136 + #138) (Claude Two-Step Prompt)

_Date: 2026-04-13 (updated — checkpoints aligned with agent prompt, enhanced review items)_
_Step: V3-A_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.0.md` — session context.
  2. `prompts/phaseV/v7.6.9.0-agent-prompt-gpt-codex.md` — full implementation spec.
  3. §1 Required Reading, focusing on:
   - `dashboard/core/status-snapshot.js` — `DEVICE_INFO_MAP` at line ~2
   - `dashboard/components/device-info/template.html` — current `di-*` IDs at lines 77–90
   - `dashboard/core/manifest.js` — how manifest data populates the dashboard

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement in this order:

  1. #144: Replace `di-mac` row with `di-device-name` row in `dashboard/components/device-info/template.html`
  2. Add `di-firmware-version` row in the same template
  3. Update `DEVICE_INFO_MAP` in `dashboard/core/status-snapshot.js` — remove `di-mac`, add new entries
  4. Populate device name and firmware version from manifest in `dashboard/core/manifest.js`

---
⛔ CHECKPOINT A (after #144 gateway card cleanup):
Run:
  grep -c "di-device-name" dashboard/components/device-info/template.html — expected: 1
  grep -c "di-firmware-version" dashboard/components/device-info/template.html — expected: 1
  grep -c "di-mac" dashboard/components/device-info/template.html — expected: 0
If ANY check fails: STOP.
---

  5. #136: Add `flash_size` and `sram_size` ESPHome text_sensor entities to C3 YAML
  6. Add `di-flash` and `di-sram` rows to template and `DEVICE_INFO_MAP`
  7. #138: Add PSRAM sensors (S3: runtime values, C3: "None" text)
  8. Add `di-psram` row to template and `DEVICE_INFO_MAP`

---
⛔ CHECKPOINT B (after #136 + #138 flash/SRAM/PSRAM):
Run:
  grep -c "di-flash\|di-sram\|di-psram" dashboard/components/device-info/template.html — expected: 3
  grep -c "flash_size\|sram_size\|psram" firmware/esp32-c3-multi-sensor.yaml — expected: ≥ 3
If ANY check fails: STOP.
---

  9. Add preflight checks for new di-* IDs
  10. Version bump, full rebuild pipeline, preflight, Playwright

---
⛔ PRE-PR GATE:
  git diff --name-only — only expected files
  bash scripts/preflight.sh — all pass
  All 5 Playwright fixture sets green
---

Do NOT edit dashboard.js or dashboard.html directly (Rule 47).
Do NOT edit dashboard.tmpl.html for device card content — edit the component template.
Do NOT edit firmware C++ fragments for this step.

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.9.0.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • `DEVICE_INFO_MAP` changes are in `dashboard/core/status-snapshot.js` (NOT in a components/ subdirectory)?
  • Manifest population is in `dashboard/core/manifest.js` (NOT in status-snapshot.js)?
  • `di-mac` row removed from template? (`grep -c "di-mac" dashboard/components/device-info/template.html` returns 0)
  • `di-device-name` and `di-firmware-version` rows added to template?
  • `devices[4]` metric_states NOT modified (only text_sensors added, no data-model changes)?
  • New text_sensor entities present in C3 YAML for `flash_size` and `sram_size`?
  • PSRAM: S3 shows runtime values, C3 shows "None"?
  • New preflight checks added for `di-device-name`, `di-firmware-version`, `di-flash`, `di-sram`?
  • Dashboard rebuild pipeline run (8 commands in correct order)?
  • No direct edits to `dashboard/dashboard.js`, `dashboard/dashboard.html`, or `dashboard/sensor_history_multi.h`?
  • No direct edits to `dashboard/dashboard.tmpl.html` (component template edited instead)?
  • No regression in env/ping/system card rendering?
  • All Playwright tests pass across all fixture sets?
  • Session log created with ESPHome output, Playwright fixture table, evidence summary?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.9.0-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.9.1.md` and `v7.6.9.1-agent-prompt-gpt-codex.md`

Operator device testing (post-merge — results go in consolidated audit):
  - Device card shows Device Name (not MAC) — verify on C3
  - Firmware Version row populated (e.g., v7.6.9.0)
  - Flash shows actual size (e.g., "4096 KB") — not hardcoded
  - SRAM shows "400 KB" on C3
  - PSRAM shows "None" on C3, shows total/free on S3

Tag: `git tag -a v7.6.9.0 -m "Phase V V3-A: Device card cleanup" && git push origin v7.6.9.0`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers. Append the step-specific focus areas from the same template's lookup table for v7.6.9.0.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.9.0 Claude two-step prompt._
