# v7.6.8.1 — History Auth + Heap Cap + DoS Cooldown + SEC-ADR (Claude Two-Step Prompt)

_Date: 2026-04-12_
_Step: V2-E + V2-F + V2-G_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.8.1.md` — session context.
  2. `prompts/phaseV/v7.6.8.1-agent-prompt-gpt-codex.md` — full spec.
  3. §1 Required Reading, focusing on history handlers and `probe_satellite_manifest_()`.

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement:

  1. Auth guard on `handle_history_()` and `handle_api_v2_history_()`
     Auth: REQUIRED — large-allocation endpoints
  2. Cap `csv.reserve()` at `std::min(est_bytes, (size_t)60000)`
  3. Add per-URL probe cooldown for add-satellite (static array, 60s window)
  4. Ensure `Docs/decisions/SEC-ADR-001-residual-vulnerabilities.md` is committed

---
⛔ CHECKPOINT A:
Run:
  grep -c "std::min.*60000" firmware/core/web-handler.h — expected: ≥ 1
  Verify history auth with grep
---

  5. Version bump, pipeline, preflight, Playwright

---
⛔ PRE-PR GATE: preflight + assembly check + all Playwright
---

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.8.1.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • History endpoints (`/history/` and `/api/v2/history/`) both have auth guards?
  • `csv.reserve()` capped at `std::min(est_bytes, (size_t)60000)` — not just `est_bytes`?
  • DoS cooldown uses static array — no heap allocation?
  • Cooldown window is 60 seconds, returns 429 on repeat?
  • SEC-ADR-001 document committed as-is — no modifications to content?
  • Auth decision comments on history handlers?
  • All Playwright tests pass?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.8.1-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.8.2.md` and `v7.6.8.2-agent-prompt-gpt-codex.md`

Operator device testing:
  - History endpoints return 401 without auth, 200 with auth
  - Two rapid add-satellite POSTs to same URL — second returns 429

Tag: `git tag -a v7.6.8.1 -m "Phase V V2-E/F/G: History auth, heap cap, DoS cooldown, SEC-ADR" && git push origin v7.6.8.1`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers (Codex, GPT, Copilot). Append the step-specific focus areas from the same template's lookup table for v7.6.8.1.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.8.1 Claude two-step prompt._
