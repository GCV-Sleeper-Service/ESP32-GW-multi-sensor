# v7.6.9.4 — Heap-Adaptive History Cap + Boot Sequencing (#139 Partial Fix) (Claude Two-Step Prompt)

_Date: 2026-04-16_
_Step: V4 — first of three Phase V mitigation steps (plan addendum authorised; actual Phase V closure is v7.6.9.6)_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/claude_sleeper_service_v2

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.4.md` — session context.
  2. `Docs/phase-V-implementation-plan-addendum-v7.6.9.4.md` — authoritative scope carve-out. This is NOT a Phase 7 step. The addendum explicitly lists what is in scope and what stays deferred to Phase 7.
  3. `prompts/phaseV/v7.6.9.4-agent-prompt-gpt-codex.md` — full implementation spec with checkpoints.

Then read every file listed in the agent prompt's §1 Required Reading section (`Docs/decisions/SEC-ADR-001-residual-vulnerabilities.md` RV-03; `firmware/core/web-handler.h` at both `csv.reserve` sites; `dashboard/core/boot.js` lines 80–120; `dashboard/core/status-snapshot.js` `loadStatusSnapshot()`; `dashboard/components/live-view/index.js` `loadHistory()` idempotency guard; `firmware/core/config.h`).

### Scope discipline

This step is authorised by the plan addendum to deliver **two specific mitigations** and nothing else:

1. Server-side heap-adaptive reserve cap (`clamp(esp_get_free_heap_size()/3, 12000, 60000)`) at two sites in `firmware/core/web-handler.h`
2. Client-side boot sequencing: gate initial `loadHistory()` on first `loadStatusSnapshot()` + 15 s fallback

If the implementation seems to require changes to NVS format, storage engine, response framing, chunked streaming, per-device storage, partition tables, fragment boundaries, `handle_status_`, `handle_status_full_`, SEC-ADR, or history endpoint auth — STOP. Those are either Phase 7 scope or explicitly preserved. Flag the discrepancy to the operator.

### Critical invariants (must hold at merge)

- `/api/status` response body stays minimal per SEC-ADR RV-03 (no heap/uptime/version fields added back)
- History endpoints stay public GET (no `authenticate_management_()` re-added — hotfix 1 trade-off preserved)
- No direct edits to `dashboard/sensor_history_multi.h` (Rule 58)
- No direct edits to `dashboard/dashboard.js` / `dashboard.html` / `dashboard.h` (Rule 47)
- No fragment boundary reorganisation (Rule 62)
- Adaptive-cap formula hard-coded inline at both call sites; no helper function, no configurable macros
- `esp_get_free_heap_size()` is the correct heap query — not `esp_get_minimum_free_heap_size()` (low watermark) and not `heap_caps_get_free_size(MALLOC_CAP_INTERNAL)` (excludes PSRAM on S3)

---

⛔ CHECKPOINT A (after server-side edits, before client-side edits):
  `grep -c "csv.reserve(std::min(est_bytes, adaptive_cap))" firmware/core/web-handler.h` — expected: 2
  `grep -c "csv.reserve(std::min(est_bytes, (size_t)60000))" firmware/core/web-handler.h` — expected: 0
  `git diff --name-only firmware/` — expected: only `firmware/core/web-handler.h`

⛔ CHECKPOINT B (after client-side edit):
  `grep -c "_v7_9_4_kickHistoryOnce" dashboard/core/boot.js` — expected: 3
  `grep -c "loadStatusSnapshot().then" dashboard/core/boot.js` — expected: 1
  old fixed 10 s trigger removed

⛔ PRE-PR GATE (before opening PR):
  preflight + assembly identity (`bash scripts/assemble-sensor-history.sh --check`) + pipeline `--check` + Playwright across all 4 fixture sets
  **PLUS:** device smoke test on all three boards (§8 of the agent prompt) MUST be filled in — this is the primary merge gate. Playwright cannot validate the adaptive cap; only hardware can.

---

⚠️ MANDATORY DEVICE SMOKE TEST BEFORE MERGE — WROOM primary gate.
  The WROOM-32D at 192.168.120.190 previously crashed on `curl /history/office/temp`. The adaptive cap is the fix. Confirm:
  (a) `curl /history/office/temp` returns 200 with CSV ≤ 12 KB
  (b) board does not reboot after 3 consecutive history fetches
  (c) 10-minute dashboard soak keeps `min_free_heap` ≥ 15 KB

If WROOM still crashes or `min_free_heap` drops below 15 KB: STOP, do not merge. Candidate follow-ups (need new agent session):
- Lower floor from 12000 to 8000
- Change divisor from 3 to 4
- Add a `min_free_heap < 15000` → early-return 503 guard

---

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/claude_sleeper_service_v2

Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.9.4.md` and `Docs/phase-V-implementation-plan-addendum-v7.6.9.4.md` to understand the current stage, scope carve-out rationale, and explicit out-of-scope items.

After the agent creates its PR, open a second fresh session and review specifically checking:

### Scope discipline (primary review gate)

  • No changes to `SegmentSnapshot`, `HistoryMeta`, `PERSIST_SLOTS`, `HISTORY_HOURS`, `HISTORY_INTERVAL_MINUTES`, or any NVS-related struct or constant?
  • No changes to partition tables or `firmware/core/config.h` heap/retention constants?
  • No fragment boundary reorganisation (Rule 62)?
  • No changes to `handle_status_` — `/api/status` response body unchanged (SEC-ADR RV-03 intact)?
  • No changes to `handle_status_full_` field list?
  • No `authenticate_management_()` re-added to history handlers?
  • No direct edits to `dashboard/sensor_history_multi.h`, `dashboard/dashboard.js`, `dashboard/dashboard.html`, or `dashboard/dashboard.h`?
  • No SEC-ADR file changes?
  • No response framing changes or chunked/paged/windowed endpoints?

### Implementation correctness

  • Adaptive cap formula present at BOTH call sites (`handle_api_v2_history_` and `handle_history_`), hard-coded identically?
  • Formula: `clamp(esp_get_free_heap_size() / 3, 12000, 60000)` — floor 12000, ceiling 60000, divisor 3?
  • Uses `esp_get_free_heap_size()` — NOT `esp_get_minimum_free_heap_size()` or `heap_caps_get_free_size(MALLOC_CAP_INTERNAL)`?
  • Boot.js uses the idempotent `_v7_9_4_kickHistoryOnce` pattern with guard variable?
  • Primary trigger: `loadStatusSnapshot().then(...)` chains a 1 s delayed kick?
  • Fallback trigger: 15 s timer via `historyBootstrapTimerId`?
  • `historyBootstrapTimerId` variable binding preserved (other code may `clearTimeout` it)?
  • `isImportActive()` check preserved in the kick function?

### Documentation

  • `Docs/changelog.md` v7.6.9.4 entry present with Fixed + Related + Phase V Closure subsections?
  • `Docs/lessons/bugs-and-lessons-learned.md` has new LESSON-OPS entry with sequential number and heap-adaptive-cap rationale?
  • `Docs/phase-V-implementation-plan.md` amendments applied to lines 44, 805, 1223, and 1241?
  • `prompts/handoff/phaseV-results.md` v7.6.9.4 row filled?
  • Session log (`Docs/session-log-<DATE>-v7.6.9.4.md`) present with device smoke test results (not empty placeholders)?

### Device test gate

  • §8 device verification table in the session log filled with actual measured values — NOT placeholders?
  • WROOM `/history/office/temp` confirmed returning 200 with CSV ≤ 12 KB?
  • WROOM not rebooting after 3 consecutive fetches?
  • 10-minute soak test confirms `min_free_heap` ≥ 15 KB?
  • C3 `httpd_stack_watermark_bytes` has not regressed below the v7.6.9.0 baseline (~644 B from hotfix 2 measurements)?
  • All three boards still show SRAM/Flash/PSRAM/Free Heap/Uptime correctly (hotfix 2 preserved)?

### Issue #139 disposition

  • PR comment on #139 indicates partial fix, references v7.6.9.4 PR?
  • Issue stays OPEN — not closed? (closure is Phase 7's job)
  • Labels `bug`, `memory`, `esp32-c3` applied?
  • Milestone v7.6.9.x applied?

### PR hygiene

  • Instruction Compliance Output table in PR description mapping §6 acceptance criteria to code?
  • Validation evidence present (preflight output, Playwright counts, checkpoint grep outputs)?
  • VERSION = `7.6.9.4`?
  • Commit message follows repo convention (feat/fix prefix + #139 reference)?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.9.4-PR<NN>-consolidated-audit-and-lessons.md` (template: `prompts/phaseV/consolidated-audit-template-phaseV.md`; questions: `prompts/phaseV/pr-audit-question-template-phaseV.md`)
  - Fill in `prompts/handoff/phaseV-results.md` with v7.6.9.4 row (if not already done in the PR)
  - Update `prompts/phaseV/phaseV-conclusion-assessment.md` with the V4 deviation and rationale — note that Phase V actual closure remains at v7.6.9.6, NOT this step. v7.6.9.4 is the first of three mitigations.
  - Run `prompts/phaseV/phaseV-closure-analysis-prompt.md` in a Claude session
  - On GitHub issue #139: apply labels, milestone, comment referencing the PR, keep issue OPEN

Tag: `git tag -a v7.6.9.4 -m "Phase V V4: heap-adaptive history cap + boot sequencing (#139 partial)" && git push origin v7.6.9.4`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers. Append the following step-specific supplement for v7.6.9.4:

> **v7.6.9.4 focus areas — this is a scope-constrained step.**
>
> This step is authorised by `Docs/phase-V-implementation-plan-addendum-v7.6.9.4.md` to deliver exactly two mitigations for issue #139: a server-side heap-adaptive `csv.reserve()` cap in `firmware/core/web-handler.h` and a client-side boot-sequencing change in `dashboard/core/boot.js`. Anything beyond this scope belongs to Phase 7 (v7.7.x) or is preserved by earlier ADRs.
>
> **Blocking issues to surface:**
> 1. Any change to NVS format, storage engine, per-device keys, partition tables, or fragment boundaries — Phase 7 scope violation.
> 2. Any change to `handle_status_` (SEC-ADR RV-03) or re-addition of `authenticate_management_()` to history handlers (hotfix 1 trade-off).
> 3. Any change to response framing (chunked/paged/windowed) — this is the full #139 fix and belongs in Phase 7.
> 4. Adaptive-cap formula not applied identically at both call sites (`handle_api_v2_history_` and `handle_history_`).
> 5. Use of `esp_get_minimum_free_heap_size()` instead of `esp_get_free_heap_size()` — the former is a historic watermark, not current availability.
> 6. `loadHistory()` called more than once at boot (idempotency should be preserved by `_historyInFlight` guard, but double-kick is a code smell).
> 7. Issue #139 closed on merge — it is a partial fix; issue stays open.
>
> **High-priority findings to flag:**
> - Hard-coded magic numbers (12000, 60000, divisor 3) without comment rationale tying back to the addendum
> - Missing 15 s fallback trigger in `boot.js`
> - `historyBootstrapTimerId` variable binding dropped (would break any code that `clearTimeout`s it)
> - Device smoke test table in the session log with placeholder values instead of measured ones
>
> **Device test gate (the primary merge gate):** confirm the PR description or session log shows the WROOM satellite returning 200 with truncated CSV (≤ 12 KB) on `/history/office/temp`, not crashing after 3 consecutive fetches, and maintaining `min_free_heap ≥ 15 KB` after a 10-minute dashboard soak. If any of those are missing or failing, this is a Blocking finding.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.9.4 Claude two-step prompt._
