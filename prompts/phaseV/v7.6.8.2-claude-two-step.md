# v7.6.8.2 — Gated Optimisations — Sockets, Ping Stack, httpd Stack (Claude Two-Step Prompt)

_Date: 2026-04-12_
_Step: V2-H + V2-I + V2-J_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.8.2.md` — session context with measurement results.
  2. `prompts/phaseV/v7.6.8.2-agent-prompt-gpt-codex.md` — full spec with decision tables.
  3. Operator measurement results (MUST be provided before execution).

Then read every file listed in the agent prompt's §1 Required Reading section.

⚠️ ALL CHANGES ARE GATED. Each change requires measurement results.

  1. V2-H: Change LWIP sockets 18→15 ONLY IF 5-min two-tab test passes (never below 13)
  2. V2-I: Reduce ping stack 4096→2048 ONLY IF Step 7 watermark shows ≥512 B headroom
  3. V2-J: Right-size httpd stack per decision table ONLY IF Step 6 watermark allows (never below measured_peak + 2048)
  4. Update `patch-esphome-httpd-stack.sh` if httpd stack changed
  5. Version bump, pipeline, Playwright

---
⛔ PRE-PR GATE: verify each gate result documented, preflight, Playwright
---

If no gates pass: produce a no-change PR documenting measurement results.

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.8.2.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • CRITICAL: Every change is GATED — measurement results documented before code changes?
  • If any gate did NOT pass: corresponding change was NOT made (no-change path)?
  • httpd stack never set below measured_peak + 2048?
  • ping_adapter stack never set below measured_peak + 512?
  • LWIP socket count never reduced below 13 (LESSON-OPS-051)?
  • `patch-esphome-httpd-stack.sh` updated if httpd stack value changed?
  • Results documented in issues #164 and #165?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.8.2-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.9.0.md` and `v7.6.9.0-agent-prompt-gpt-codex.md`
  - Record post-V2 heap baseline for V3-F gate

Operator device testing (if any gate passed):
  - V2-H: 5-min two-tab test, zero ENFILE errors
  - V2-I: Ping sensor reports correctly for 30 minutes
  - V2-J: All dashboard operations work under new stack

Tag: `git tag -a v7.6.8.2 -m "Phase V V2-H/I/J: Gated optimisations" && git push origin v7.6.8.2`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers (Codex, GPT, Copilot). Append the step-specific focus areas from the same template's lookup table for v7.6.8.2.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.8.2 Claude two-step prompt._
