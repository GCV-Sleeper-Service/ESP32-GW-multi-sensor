# v7.6.8.2 — Socket Reduction V2-H Only (Claude Two-Step Prompt)

_Date: 2026-04-15 (updated with concrete gate results — V2-I/J blocked)_
_Step: V2-H only (V2-I and V2-J blocked by measurement results)_

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.8.2.md` — gate results are concrete, not conditional.
  2. `prompts/phaseV/v7.6.8.2-agent-prompt-gpt-codex.md` — full spec.

Then implement:

  1. Change `CONFIG_LWIP_MAX_SOCKETS: "18"` → `"15"` in `firmware/esp32-c3-multi-sensor.yaml`
  2. Version bump: `bash scripts/bump-version.sh 7.6.8.2` then `bash scripts/provision.sh satellite`
  3. Preflight, Playwright
  4. Changelog entry and session log

---
⛔ CHECKPOINT:
  grep CONFIG_LWIP_MAX_SOCKETS firmware/esp32-c3-multi-sensor.yaml — expected: "15"
  grep "xTaskCreate.*ping" firmware/core/ping-adapter.h — expected: still 4096 (V2-I BLOCKED)
  grep "stack_size" firmware/local_components/web_server_idf/web_server_idf.cpp — expected: still 16384 (V2-J BLOCKED)
---

Do NOT change ping-adapter.h (V2-I BLOCKED — watermark 2,160 B, target 2,048 unsafe).
Do NOT change web_server_idf.cpp (V2-J BLOCKED — watermark 260 B, no headroom).
Do NOT reduce LWIP below 13 (LESSON-OPS-051).

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

After the agent creates its PR, review checking:

  • `CONFIG_LWIP_MAX_SOCKETS` is `"15"` (not lower)
  • ping-adapter.h stack is UNCHANGED at 4096
  • web_server_idf.cpp stack is UNCHANGED at 16384
  • patch-esphome-httpd-stack.sh is UNCHANGED
  • Gate results documented in session log with exact measurement values
  • Preflight and Playwright pass

Post-merge deliverables:
  - `prompts/phaseV/v7.6.8.2-PR<NN>-consolidated-audit-and-lessons.md`
  - Document in issues #164 and #165
  - Review and update `session-handoff-v7.6.9.0.md`

Operator device testing:
  - Flash C3, open dashboard in two tabs (SSE + polling), monitor 5 min for ENFILE errors
  - If errors appear: revert to 18, document as V2-H blocked

Tag: `git tag -a v7.6.8.2 -m "Phase V V2-H: Socket reduction 18→15 (V2-I/J blocked)" && git push origin v7.6.8.2`

---

_End of v7.6.8.2 Claude two-step prompt._
