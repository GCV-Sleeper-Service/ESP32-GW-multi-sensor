# v7.6.9.5 — C3 httpd Stack Override Fix (Claude Two-Step Prompt)

_Date: 2026-04-20_
_Step: V5 — second of three Phase V mitigation steps (actual Phase V closure is v7.6.9.6)_
_This is the CORRECTED version. PR#195 (architecture-conditional bump) failed device gate. Root cause: missing external_components in C3 template YAML._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.5.md` — corrected root cause.
  2. `prompts/phaseV/v7.6.9.5-agent-prompt-gpt-codex.md` — full implementation spec.

Then read every file in §1 Required Reading (C3 template lines 108–116, local override line 123–124, `render_sensor_config.py` lines 1425–1434, preflight script).

### Scope discipline

This step fixes a **configuration bug**, not an architecture problem. It delivers:

1. `external_components` block added to the C3 template YAML
2. Preflight guard preventing future recurrence
3. Stress test script (retained from PR#195)
4. BUG entry, LESSON-OPS on verify-before-theorize, corrected capacity study, changelog

**There is NO architecture-conditional code.** No `#ifdef CONFIG_IDF_TARGET_ARCH_RISCV`. The stack stays at 16384 for all boards. If you see architecture-conditional code left over from PR#195, remove it.

### Critical invariants

- `firmware/local_components/web_server_idf/web_server_idf.cpp` is UNCHANGED — stack value 16384 is correct
- `scripts/patch-esphome-httpd-stack.sh` is UNCHANGED
- No `firmware/core/web-handler.h` changes
- No dashboard source changes
- No SEC-ADR changes (v7.6.9.6 scope)

---

⛔ CHECKPOINT A (after C3 template edit):
  `grep -c 'external_components' firmware/esp32-c3-multi-sensor.yaml` — expected: 1
  `grep -n '^external_components:' firmware/esp32-c3-multi-sensor.yaml` — expected: one line at top level

⛔ CHECKPOINT B (after preflight guard):
  `bash scripts/preflight.sh` — expected: passes including new external_components check

⛔ PRE-PR GATE:
  preflight + assembly + pipeline + Playwright all green.
  **PLUS:** C3 watermark ≥ 10000 bytes (proving override is active). If still ~636 → STOP.
  No `CONFIG_IDF_TARGET_ARCH_RISCV` anywhere in firmware/.

---

⚠️ MANDATORY DEVICE TEST BEFORE MERGE — C3 primary gate.
  The C3 was running on stock 4 KB httpd stack (watermark 636). After adding external_components,
  it should have ~12,900 B headroom on the 16 KB stack. Confirm:
  (a) Flash C3, check `/api/status/full` → `httpd_stack_watermark_bytes` ≥ 10000
  (b) Run `bash scripts/stress-test-httpd-stack.sh 192.168.120.189` — minimum ≥ 10000
  (c) WROOM and S3 watermarks unchanged

If C3 watermark is still ~636: the override is not compiling in. Check:
  `grep -r 'stack_size' firmware/.esphome/build/*/src/esphome/components/web_server_idf/ 2>/dev/null`

---

## Step 2 — Review Session

After agent creates PR, review checking:

### Scope discipline
  • `external_components` added to C3 template at top-level indentation?
  • No changes to `firmware/local_components/` (override file unchanged)?
  • No `CONFIG_IDF_TARGET_ARCH_RISCV` anywhere? (Should be 0 occurrences)
  • No changes to `firmware/core/web-handler.h`?
  • No dashboard source changes?
  • No SEC-ADR changes?
  • Stack value in override still exactly 16384? (Not 20480, not conditional)

### Documentation
  • BUG entry documents the missing external_components as root cause?
  • LESSON-OPS documents verify-before-theorize rule?
  • Changelog correctly describes a config fix, not an architecture finding?
  • Capacity study corrected — no architecture-conditional language?

### Device test gate
  • C3 watermark ≥ 10000 in session log (not ~636)?
  • WROOM and S3 watermarks unchanged?
  • Stress test minimum ≥ 10000 on C3?

### PR hygiene
  • VERSION = 7.6.9.5?
  • PR title references the real root cause (missing external_components)?

Post-merge deliverables:
  - Consolidated audit
  - Fill `phaseV-results.md` v7.6.9.5 row
  - Issue #165 comment with corrected findings
  - Do NOT run Phase V closure — waits for v7.6.9.6

Tag: `git tag -a v7.6.9.5 -m "Phase V V5: Fix C3 missing httpd stack override" && git push origin v7.6.9.5`

---

## External Reviewer Supplement

> **v7.6.9.5 focus: configuration bug fix, not architecture change.**
>
> The C3 template YAML was missing `external_components` — the httpd stack override never compiled in. Fix is 4 lines of YAML.
>
> **Blocking issues:**
> 1. Any architecture-conditional code (`#ifdef CONFIG_IDF_TARGET_ARCH_RISCV`) — not needed
> 2. Stack value changed from 16384 — should stay as-is
> 3. Any changes to `firmware/local_components/` — the override is correct, only the wiring was missing
> 4. C3 watermark still ~636 in session log — means the fix didn't take effect
>
> **Key check:** `grep -c 'external_components' firmware/esp32-c3-multi-sensor.yaml` must be 1.

---

_End of v7.6.9.5 Claude two-step prompt._
