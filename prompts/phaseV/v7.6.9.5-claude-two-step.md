# v7.6.9.5 — C3 httpd Stack Watermark Investigation (Claude Two-Step Prompt)

_Date: 2026-04-19_
_Step: V5 — second of three Phase V mitigation steps (actual Phase V closure is v7.6.9.6)_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.5.md` — session context.
  2. `Docs/phase-V-implementation-plan-addendum-v7.6.9.4.md` — the addendum authorising v7.6.9.5 (line 61: "C3 `httpd_stack_watermark_bytes` investigation").
  3. `prompts/phaseV/v7.6.9.5-agent-prompt-gpt-codex.md` — full implementation spec with checkpoints.

Then read every file listed in the agent prompt's §1 Required Reading section (`firmware/local_components/web_server_idf/web_server_idf.cpp` line 123–124; `firmware/local_components/web_server_idf/PATCH_INFO.md`; `scripts/patch-esphome-httpd-stack.sh` completely; `Docs/lessons/firmware.md` LESSON-OPS-100 through 102; `firmware/core/web-handler.h` watermark measurement at line 1384).

### Scope discipline

This step is an **investigation step** with a narrow code deliverable. It delivers:

1. A stress test script at `scripts/stress-test-httpd-stack.sh`
2. A conditional httpd stack bump: 20480 bytes for C3 (RISC-V), 16384 for Xtensa targets
3. Updated patch script and PATCH_INFO documentation
4. Changelog, LESSON-OPS entry, session log scaffold

If the implementation seems to require changes to handler bodies, dashboard code, NVS format, SEC-ADR, partition tables, response framing, or auth posture — STOP. Those are all out of scope.

### Critical invariants (must hold at merge)

- `firmware/core/web-handler.h` is UNCHANGED — no handler edits, no watermark reporting changes
- No `dashboard/` source files changed
- `/api/status` response body stays minimal per SEC-ADR RV-03 (v7.6.9.6 scope)
- No fragment boundary changes (Rule 62)
- Stack size uses `#ifdef CONFIG_IDF_TARGET_ARCH_RISCV` — not a uniform bump, not runtime detection
- WROOM and S3 stacks stay at 16384 — the WROOM cannot afford 4 KB less free heap

---

⛔ CHECKPOINT A (after stress test script creation):
  `test -x scripts/stress-test-httpd-stack.sh` — expected: OK
  No other files changed yet

⛔ CHECKPOINT B (after conditional stack bump):
  `grep -c 'stack_size = 20480' firmware/local_components/web_server_idf/web_server_idf.cpp` — expected: 1
  `grep -c 'stack_size = 16384' firmware/local_components/web_server_idf/web_server_idf.cpp` — expected: 1
  `grep -c 'CONFIG_IDF_TARGET_ARCH_RISCV' firmware/local_components/web_server_idf/web_server_idf.cpp` — expected: 1

⛔ CHECKPOINT D (after patch script update):
  `grep -c 'CONFIG_IDF_TARGET_ARCH_RISCV' scripts/patch-esphome-httpd-stack.sh` — expected: ≥ 2
  `grep -c '20480' scripts/patch-esphome-httpd-stack.sh` — expected: ≥ 2

⛔ PRE-PR GATE (before opening PR):
  preflight + assembly identity (`bash scripts/assemble-sensor-history.sh --check`) + pipeline `--check` + Playwright across all 4 fixture sets
  **PLUS:** device stress test on C3 with bumped firmware (§8 of agent prompt) MUST be filled in — this is the primary merge gate. Playwright cannot validate FreeRTOS stack sizing.

---

⚠️ MANDATORY DEVICE STRESS TEST BEFORE MERGE — C3 primary gate.
  The C3 at 192.168.120.189 has `httpd_stack_watermark_bytes` = 636 on the current 16 KB stack. The 20 KB bump should bring this to ~4700+. Confirm:
  (a) Flash C3 with v7.6.9.5 firmware
  (b) Run `bash scripts/stress-test-httpd-stack.sh 192.168.120.189`
  (c) Minimum watermark across 5 waves ≥ 2000 bytes
  (d) WROOM and S3 watermark not regressed (smoke test)

If C3 stress test watermark < 2000: STOP, do not merge. Bump to 24576 (24 KB) in a new agent session and retest.

---

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.9.5.md` and `Docs/phase-V-implementation-plan-addendum-v7.6.9.4.md` to understand the current stage and scope.

After the agent creates its PR, open a second fresh session and review specifically checking:

### Scope discipline (primary review gate)

  • No changes to `firmware/core/web-handler.h`? (The entire file must be unchanged — verify with `git diff --name-only main -- firmware/core/`)
  • No changes to any `dashboard/` source files? (`dashboard/core/`, `dashboard/components/`)
  • No changes to `Docs/decisions/SEC-ADR-001-residual-vulnerabilities.md`?
  • No changes to partition tables?
  • No changes to NVS-related files?
  • No fragment boundary changes?
  • No changes to auth posture on any endpoint?

### Implementation correctness

  • Stack bump uses `#ifdef CONFIG_IDF_TARGET_ARCH_RISCV` — not a uniform value?
  • C3 target value is exactly 20480?
  • Xtensa fallback value is exactly 16384 (unchanged from before)?
  • The `#if / #else / #endif` block is inside `AsyncWebServer::begin()`, immediately after `HTTPD_DEFAULT_CONFIG()`?
  • No other changes to `web_server_idf.cpp` besides the stack size line(s)?
  • Patch script correctly applies the conditional (Python heredoc insertion, not sed single-line)?
  • Patch script `--check` mode validates both `CONFIG_IDF_TARGET_ARCH_RISCV` and both stack size values?
  • Patch script removes the old single-line patch before inserting the conditional (handles re-run after upgrade)?
  • Stress test script is executable, targets 5 waves × 8 concurrent requests, and reports PASS/FAIL against ≥ 2000 threshold?

### Documentation

  • `Docs/changelog.md` v7.6.9.5 entry present with Investigation Findings section?
  • `Docs/lessons/firmware.md` has new LESSON-OPS entry with sequential number and RISC-V vs Xtensa explanation?
  • `Docs/lessons/index.md` updated with the new entry?
  • `PATCH_INFO.md` documents the conditional stack sizing and v7.6.9.5 context?
  • Session log (`Docs/session-log-*-v7.6.9.5.md`) present with device stress test results (not empty placeholders)?

### Device test gate

  • C3 stress test minimum watermark ≥ 2000 bytes (§8 Phase B results in session log)?
  • WROOM and S3 watermark values similar to pre-bump (13044 and 13760 ± small variation)?
  • WROOM `free_heap` not significantly lower than pre-bump (~37032)?
  • All three boards show correct `version` = `v7.6.9.5`?
  • No watchdog reboots during testing?
  • `/api/status` still returns only `{ok, role, id}` on all boards (SEC-ADR RV-03)?

### PR hygiene

  • Instruction Compliance Output table in PR description mapping §6 acceptance criteria to code?
  • Validation evidence present (preflight output, Playwright counts, stress test output, checkpoint grep outputs)?
  • VERSION = `7.6.9.5`?
  • Commit message follows repo convention (feat/fix prefix + #165 reference)?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.9.5-PR<NN>-consolidated-audit-and-lessons.md` (template: `prompts/phaseV/consolidated-audit-template-phaseV.md`; questions: `prompts/phaseV/pr-audit-question-template-phaseV.md`)
  - Fill in `prompts/handoff/phaseV-results.md` with v7.6.9.5 row (if not already done in the PR)
  - On GitHub issue #165: post comment with stack investigation findings and PR reference
  - Do NOT run Phase V closure analysis — that waits for v7.6.9.6
  - Do NOT update `phaseV-conclusion-assessment.md` — v7.6.9.6 handles closure

Tag: `git tag -a v7.6.9.5 -m "Phase V V5: C3 httpd stack bump — RISC-V conditional 20 KB (#165)" && git push origin v7.6.9.5`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers. Append the following step-specific supplement for v7.6.9.5:

> **v7.6.9.5 focus areas — this is an investigation step with a narrow code change.**
>
> This step investigated the C3 httpd stack watermark (636 bytes remaining on a 16 KB stack) and applied a conditional stack bump to 20 KB for RISC-V targets. Xtensa targets (WROOM, S3) stay at 16 KB because their watermark is ~13 KB (ample headroom) and the WROOM cannot afford 4 KB less free heap.
>
> **Blocking issues to surface:**
> 1. Any change to `firmware/core/web-handler.h` — handlers must be unchanged.
> 2. Any change to `dashboard/` source files — no dashboard work in this step.
> 3. Any change to `handle_status_` or SEC-ADR files — v7.6.9.6 scope.
> 4. A uniform stack bump (same value for all architectures) instead of conditional `#ifdef` — wastes 4 KB of WROOM heap.
> 5. Stack bump value below 20480 without stress test evidence.
> 6. Runtime chip detection instead of compile-time `#ifdef` — unnecessary overhead.
> 7. Any NVS, partition, or response framing changes — Phase 7 scope.
>
> **High-priority findings to flag:**
> - Patch script's `--check` mode not validating the conditional (would silently pass with the old single-line patch)
> - Patch script not cleaning up old single-line patch on re-run (would leave both old and new patches in the file)
> - Stress test script missing the PASS/FAIL threshold check or the concurrent request wave pattern
> - LESSON-OPS entry missing the architecture explanation (RISC-V vs Xtensa register windows)
> - Session log with placeholder device test values instead of measured ones
>
> **Device test gate (the primary merge gate):** confirm the PR description or session log shows the C3 stress test minimum watermark ≥ 2000 bytes on the 20 KB stack. If the stress test wasn't run or the result is missing, this is a Blocking finding. Also confirm WROOM and S3 watermark values haven't regressed.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.9.5 Claude two-step prompt._
