# v7.6.8.0 — Auth Guards + Status Field Strip (Claude Two-Step Prompt)

_Date: 2026-04-15 (updated with v7.6.7.3 telemetry context and corrected field lists)_
_Step: V2-A + V2-B + V2-C + V2-D_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.8.0.md` — session context, v7.6.7.3 telemetry fields, V1 measurement results, exhaustive `/api/status` field list.
  2. `prompts/phaseV/v7.6.8.0-agent-prompt-gpt-codex.md` — full spec including V2 threat model.
  3. §1 Required Reading, focusing on all endpoints being auth-guarded, the `authenticate_management_()` pattern, and the complete `handle_status_()` function (which has v7.6.7.3 telemetry fields that must be preserved in `/api/status/full`).

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement:

  1. Add `authenticate_management_()` as first line of `handle_api_ingest_()` (V2-A)
     Auth: REQUIRED — write endpoint, prevents unauthenticated data injection
  2. Remove LESSON-OPS-089 exception comment from `handle_add_satellite_()`
  3. Add auth guard to `handle_add_satellite_()` (V2-B)
     Auth: REQUIRED — topology-modifying write endpoint

---
⛔ CHECKPOINT A (after V2-A/B):
Run:
  grep -c "LESSON-OPS-089" firmware/core/web-handler.h — expected: 0
  grep -A2 "handle_api_ingest_" firmware/core/web-handler.h | grep -c "authenticate_management_" — expected: 1
---

  4. Auth guard on `handle_aggregator_gateways_()`, `handle_aggregator_live_()`, `handle_aggregator_proxy_()` (V2-C)
  5. Replace `handle_status_()` body — return ONLY `{"ok":true,"role":"...","id":"..."}` using `beginResponse` (NOT beginResponseStream) (V2-D)
     - `role` from `AGGREGATOR_ENABLED`: "aggregator" or "satellite"
     - `id` from `App.get_name().c_str()`
  6. Add auth-gated `handle_status_full_()` returning ALL current fields including v7.6.7.3 telemetry (min_free_heap, httpd_stack_watermark_bytes, ping_stack_watermark_bytes), plus new `role` and `id` fields. **Must use pre-reserved `std::string` + `beginResponse` — Rule 8 prohibits `beginResponseStream` in new handlers.** (V2-D)
  7. Add `basic_auth` parameter to `fetch_to_buffer()` (V2-D)
  8. Update aggregator polling to use `/api/status/full` with credentials (V2-D)

---
⛔ CHECKPOINT B (after V2-C/D):
Run:
  grep -c "api/status/full" firmware/core/web-handler.h — expected: ≥ 3
  grep -c "basic_auth" firmware/core/aggregator-runtime.h — expected: ≥ 2
  grep -A 80 "handle_status_full_" firmware/core/web-handler.h | grep -c "min_free_heap" — expected: 1
  bash scripts/assemble-sensor-history.sh --write && --check — PASS
---

  9. Add LESSON-SEC-001 to `Docs/lessons/build-pipeline.md`
  10. Mark LESSON-OPS-089 as resolved
  11. Update test fixtures — public `/api/status` shape changed to `{ok, role, id}`
  12. Version bump: `bash scripts/bump-version.sh 7.6.8.0` then `bash scripts/provision.sh satellite`
  13. Preflight, Playwright

---
⛔ PRE-PR GATE:
  All expected files only, preflight passes, all Playwright green
---

LESSON-OPS-110: Every handler code block MUST have `Auth: REQUIRED` or `Auth: NOT REQUIRED` comment.
Do NOT add auth to `/api/status` (strip fields instead — public health check endpoint).
Do NOT add auth to `/api/manifest`, `/sensors.json`, `/api/v2/live` — accepted risk per SEC-ADR-001.
Do NOT use `beginResponseStream` in any new handler — Rule 8.
Do NOT remove v7.6.7.3 telemetry fields — they must be preserved in `/api/status/full`.

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.8.0.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • CRITICAL: Every modified handler has `authenticate_management_()` as absolute FIRST line?
  • LESSON-OPS-089 exception comment REMOVED from `handle_add_satellite_()`?
  • `/api/status` public response contains ONLY `ok`, `role`, `id` — no `version`, `free_heap`, `uptime_seconds`, `sensors`, `min_free_heap`, `httpd_stack_watermark_bytes`, or any other field?
  • `/api/status/full` requires auth and returns ALL fields: `ok`, `role`, `id`, `version`, `uptime_seconds`, `sensor_count`, `sensors[]`, `ram_history_points_per_series`, `persist_days`, `free_heap`, `free_heap_internal`, `free_heap_total`, `min_free_heap`, `httpd_stack_watermark_bytes`, `ping_stack_watermark_bytes`?
  • `/api/status/full` does NOT use `beginResponseStream` (Rule 8)?
  • `fetch_to_buffer()` has `basic_auth` parameter correctly wired?
  • Aggregator polling updated to call `/api/status/full` with credentials?
  • Auth decision comment (LESSON-OPS-110) present on every modified endpoint handler?
  • LESSON-SEC-001 added to lessons?
  • LESSON-OPS-089 marked as resolved?
  • No auth added to `/api/status`, `/api/manifest`, `/sensors.json`, `/api/v2/live`?
  • Dashboard still handles 401 correctly (no JS changes needed)?
  • Test fixtures updated for new public `/api/status` shape?
  • All Playwright tests pass?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.8.0-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.8.1.md` and `v7.6.8.1-agent-prompt-gpt-codex.md`

Operator device testing (CRITICAL — test both C3 and S3):
  - Unauthenticated ingest returns 401
  - Unauthenticated add-satellite returns 401
  - Unauthenticated gateways returns 401
  - Public `/api/status` returns only {ok, role, id} — no other fields
  - `/api/status/full` requires auth, returns full JSON with ALL fields including v7.6.7.3 telemetry
  - Aggregator dashboard still shows satellite info (via /api/status/full)

Tag: `git tag -a v7.6.8.0 -m "Phase V V2-A/B/C/D: Auth guards, status split" && git push origin v7.6.8.0`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers (Codex, GPT, Copilot). Append the step-specific focus areas from the same template's lookup table for v7.6.8.0.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.8.0 Claude two-step prompt._
