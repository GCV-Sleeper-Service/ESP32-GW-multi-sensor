# v7.6.7.2 — Version Badge + Dead Code Deletion + Import Comment (Claude Two-Step Prompt)

_Date: 2026-04-12_
_Step: V1-E + V1-F + V1-G_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.7.2.md` — session context.
  2. `prompts/phaseV/v7.6.7.2-agent-prompt-gpt-codex.md` — full spec.
  3. §1 Required Reading, focusing on `dashboard/dashboard.tmpl.html` (footer area), `dashboard/core/app-shell.js` (Boot section), dead code locations in `nvs-persistence.h` and `data-model.h`.

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement:

  1. Add `<span id="versionBadge" ...>` to `dashboard/dashboard.tmpl.html` footer
  2. Populate badge from `App.version` in `app-shell.js` Boot section (before SSE)
  3. Add `dashboard_has_version_badge` preflight check

---
⛔ CHECKPOINT A (after badge):
Run:
  grep -c "versionBadge" dashboard/dashboard.tmpl.html — expected: 1
  grep -c "versionBadge" dashboard/core/app-shell.js — expected: ≥ 1
---

  4. Confirm zero call sites: `grep -rn "stream_snapshot_series_\|->stream_to(" firmware/` (ONLY definitions, ZERO external callers)
  5. Delete `stream_snapshot_series_()` from `nvs-persistence.h`
  6. Delete `HistoryBuffer::stream_to()` from `data-model.h`

---
⛔ CHECKPOINT B (after dead code deletion):
Run:
  grep -rn "stream_snapshot_series_\|->stream_to(" firmware/ — expected: 0 results
  bash scripts/assemble-sensor-history.sh --check — PASS
---

  7. Add import session timeout comment at `handle_import_begin_()` (V1-G)
  8. Dashboard rebuild pipeline (bundle → build → minify → header)
  9. Version bump, preflight, Playwright

---
⛔ PRE-PR GATE:
  git diff --name-only — only expected files
  bash scripts/preflight.sh — all pass (including new badge check)
  All Playwright fixture sets green
---

Do NOT edit dashboard.js or dashboard.html directly (Rule 47). Do NOT add auth guards.

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.7.2.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • Version badge uses `App.version` (not a hardcoded string)?
  • Badge populated before SSE setup (first action in Boot)?
  • Badge visible in both light and dark mode?
  • Dead code: `grep -rn "stream_snapshot_series_\|->stream_to(" firmware/` returns zero?
  • No direct edits to `dashboard/dashboard.js` or `dashboard/dashboard.html` — Rule 47?
  • Dashboard rebuild pipeline was run (4 commands: bundle → build → minify → header)?
  • Import comment references SEC-ADR-001 RV-05?
  • `dashboard_has_version_badge` preflight check added and passing?
  • All Playwright tests pass?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.7.2-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.8.0.md` and `v7.6.8.0-agent-prompt-gpt-codex.md`

V1 is complete after this step. Before starting V2:
  - Run the operator measurement protocol (Steps 1-7 from plan) on C3 hardware
  - Record heap and stack watermark values in the measurement table
  - These gate V2-H/I/J decisions

Operator device testing:
  - Version badge visible in dashboard footer
  - Badge appears before SSE connects

Tag: `git tag -a v7.6.7.2 -m "Phase V V1-E/F/G: Badge, dead code, import comment" && git push origin v7.6.7.2`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers (Codex, GPT, Copilot). Append the step-specific focus areas from the same template's lookup table for v7.6.7.2.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.7.2 Claude two-step prompt._
