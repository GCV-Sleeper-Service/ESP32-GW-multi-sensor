# v7.6.7.0 — Proxy Fix + NAS History Disable + Logger Level (Claude Two-Step Prompt)

_Date: 2026-04-12_
_Step: V1-A + V1-B + V1-C_

---

## When to Use

Use this prompt when Claude is the execution agent instead of GPT/Codex. Claude has project context in memory, so this prompt is shorter.

## Step 1 — Implementation

See `v7.6.7.0-agent-prompt-gpt-codex.md` for the full specification including:
- §1 Required Reading list
- §3 Scope boundary and §4 Critical Rules checklist
- §5 Implementation instructions with ⛔ CHECKPOINT gates
- §6 Acceptance criteria and §10 DO-NOT list

**Claude: read the GPT/Codex prompt, then read these source files:**

```
firmware/core/aggregator-runtime.h, firmware/core/data-model.h, firmware/core/web-handler.h, firmware/esp32-c3-multi-sensor.yaml
```

**Produce the exact code changes as diffs or complete replacement blocks for each file in scope.**

Follow all ⛔ CHECKPOINT gates. If any fails, stop and report.

**Critical Rules:** Rule 58 (edit fragments → assemble), Rule 47 (no direct dashboard edits), Rule 62 (no boundary changes). For V2 steps: LESSON-OPS-110 (auth decision comment on every handler).

---

## Step 2 — Verification

After the operator applies changes and runs the pipeline, they report:

1. `bash scripts/preflight.sh` output
2. `bash scripts/assemble-sensor-history.sh --check` output (if fragments edited)
3. Playwright test results (all 5 fixture sets)
4. `git diff --name-only` output

**Claude: verify against acceptance criteria in GPT/Codex prompt §6.**

Check: scope boundary respected, all criteria met, no Critical Rule violations, session log created, changelog present.

If issues found, produce fix or escalation recommendation.

---

_End of v7.6.7.0 Claude two-step prompt._
