# v7.6.7.0 — Proxy 502 Fix + NAS History Disable + Logger Level (Claude Two-Step Prompt)

_Date: 2026-04-12_
_Step: V1-A + V1-B + V1-C_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.7.0.md` — session context, carries-forward from Phase Y.
  2. `prompts/phaseV/v7.6.7.0-agent-prompt-gpt-codex.md` — full implementation spec.
  3. Read every file in that prompt's §1 Required Reading, paying attention to:
   - `firmware/core/aggregator-runtime.h` — `fetch_to_buffer()` at line ~124 (signature being changed)
   - `firmware/core/web-handler.h` — `handle_aggregator_proxy_()` at line ~1556 (response being fixed)
   - `firmware/core/data-model.h` — `metrics_system[]` at line ~303 and `entity_hbuf_nas01_*` at lines ~318–320

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement in this order:

  1. Modify `fetch_to_buffer()` signature — add `timeout_s` (default 5) and `out_http_status` (default nullptr)
  2. Apply `timeout_s` to `lwip_setsockopt()` calls inside `fetch_to_buffer()`
  3. Parse HTTP status code and store via `out_http_status` pointer

---
⛔ CHECKPOINT A (after signature change):
Run:
  grep -c "timeout_s" firmware/core/aggregator-runtime.h — expected: ≥ 2
  grep -c "out_http_status" firmware/core/aggregator-runtime.h — expected: ≥ 2
  Verify all existing call sites (~lines 246, 599, 646, 685) are UNCHANGED (default params)
If ANY check fails: STOP.
---

  4. Modify `handle_aggregator_proxy_()`: 502+JSON on failure, 200+empty on no-history, 15s timeout
  5. Set `history_enabled=false` for NAS metrics in `data-model.h`
  6. Delete three static `entity_hbuf_nas01_*` HistoryBuffers
  7. Set `history = nullptr` for `devices[4]` metric_states

---
⛔ CHECKPOINT B (after NAS history disable):
Run:
  grep -c "entity_hbuf_nas01_" firmware/core/data-model.h — expected: 0
  bash scripts/assemble-sensor-history.sh --write && bash scripts/assemble-sensor-history.sh --check — PASS
If ANY check fails: STOP.
---

  8. Change logger level INFO→WARN, wifi/api→ERROR in YAML
  9. Version bump, full pipeline, preflight, Playwright

---
⛔ PRE-PR GATE:
  git diff --name-only — only expected files
  bash scripts/preflight.sh — all pass
  bash scripts/assemble-sensor-history.sh --check — PASS
  All 5 Playwright fixture sets green
---

Do NOT add auth guards (V2 scope). Do NOT modify import handlers (V1-D scope).
Do NOT edit `dashboard/sensor_history_multi.h` directly (Rule 58).

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.7.0.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • `fetch_to_buffer()` signature has `timeout_s` and `out_http_status` with correct defaults?
  • All existing `fetch_to_buffer()` call sites unchanged (use defaults, no modifications)?
  • Proxy returns 502 + JSON `{"error":"upstream_fetch_failed",...}` on failure (not empty body)?
  • Proxy returns 200 + empty body when satellite has no history (not 502)?
  • Proxy passes `timeout_s=15` to `fetch_to_buffer()`?
  • `ESP_LOGW` emitted on proxy fetch failure?
  • `lwip_setsockopt` used (not `setsockopt`) for timeout — Rule 27?
  • `metrics_system[]` entries for cpu/ram/disk have `false` for history?
  • Three `entity_hbuf_nas01_*` static globals deleted? (`grep -c` returns 0)
  • `devices[4]` metric_states have `history = nullptr` (not dangling pointers)?
  • Logger level is WARN, wifi/api are ERROR?
  • `assemble-sensor-history.sh --write` was run after fragment edits?
  • No direct edits to `dashboard/sensor_history_multi.h` (Rule 58)?
  • No auth guards added (V2 scope)?
  • All Playwright tests pass across all fixture sets?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.7.0-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.7.1.md` and `v7.6.7.1-agent-prompt-gpt-codex.md`

Operator device testing (post-merge — results go in consolidated audit):
  - Proxy returns 502+JSON when satellite disconnected
  - Proxy returns 200+empty when satellite has no history
  - NAS history endpoint returns 404
  - Free heap increased ~2.3 KB vs baseline

Tag: `git tag -a v7.6.7.0 -m "Phase V V1-A/B/C: Proxy fix, NAS history disable, logger level" && git push origin v7.6.7.0`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers (Codex, GPT, Copilot). Append the step-specific focus areas from the same template's lookup table for v7.6.7.0.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.7.0 Claude two-step prompt._
