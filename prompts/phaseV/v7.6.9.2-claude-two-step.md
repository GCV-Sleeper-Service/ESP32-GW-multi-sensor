# v7.6.9.2 — Manifest-Driven Export + AGG-ADR Commit (Claude Two-Step Prompt)

_Date: 2026-04-13 (updated — checkpoint aligned with agent prompt, enhanced review items)_
_Step: V3-D + V3-E_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.2.md` — session context.
  2. `prompts/phaseV/v7.6.9.2-agent-prompt-gpt-codex.md` — full spec.
  3. §1 Required Reading, focusing on:
   - `dashboard/core/sensor-defs.js` — `EXPORT_SENSOR_SUFFIXES` at line ~13 (being replaced)
   - `dashboard/core/history.js` — `fetchSensorHistoryRows()` at line ~126, hardcoded `key === 'temp'` at line ~130

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement:

  1. Replace `EXPORT_SENSOR_SUFFIXES` static array with `getMetricColumnsForSensor()` function in `sensor-defs.js`
  2. Update all `EXPORT_SENSOR_SUFFIXES` usages (lines ~85, ~95) to use the new function
  3. Replace hardcoded `key === 'temp'` in `fetchSensorHistoryRows()` with manifest-driven iteration
  4. Update `buildSingleSensorCsv()` and `buildMergedSensorCsv()` to use `getMetricColumnsForSensor()`

---
⛔ CHECKPOINT A (after manifest-driven replacement):
Run:
  grep -c "EXPORT_SENSOR_SUFFIXES" dashboard/core/sensor-defs.js dashboard/core/history.js — expected: 0
  grep -c "getMetricColumnsForSensor" dashboard/core/sensor-defs.js — expected: ≥ 1
  grep -c "key === 'temp'" dashboard/core/history.js — expected: 0
If ANY check fails: STOP.
---

  5. Verify AGG-ADR-001 is on main (commit if missing)
  6. Version bump, full pipeline, preflight, Playwright

---
⛔ PRE-PR GATE:
  preflight + all Playwright fixture sets green
---

Do NOT modify `EXPORT_SHARED_COLUMNS` (role was added in V3-C).

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.9.2.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • `EXPORT_SENSOR_SUFFIXES` completely removed? (`grep -c` returns 0 in both files)
  • `getMetricColumnsForSensor()` function definition present in `sensor-defs.js`?
  • `getMetricColumnsForSensor()` called from `history.js` (in `fetchSensorHistoryRows()` and both CSV builders)?
  • Fallback to `['temp', 'hum']` when manifest unavailable?
  • Hardcoded `key === 'temp'` removed from `fetchSensorHistoryRows()`?
  • Ping device CSV export has `ping_ms` and `success_pct` columns populated (not blank)?
  • System device CSV export has populated metric columns (not blank)?
  • Environmental sensor export unchanged (backward compatible with V3-C format)?
  • AGG-ADR-001 document present on main?
  • `role` column and satellite prefix from V3-C still working correctly?
  • All Playwright tests pass?
  • Session log created with ESPHome output, Playwright fixture table, evidence summary?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.9.2-PR<NN>-consolidated-audit-and-lessons.md`
  - Close issues #161 (ref V1-A PR) and #162 (ref this PR)
  - Review and update `session-handoff-v7.6.9.3.md` and `v7.6.9.3-agent-prompt-gpt-codex.md`

Tag: `git tag -a v7.6.9.2 -m "Phase V V3-D/E: Manifest-driven export, AGG-ADR" && git push origin v7.6.9.2`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers. Append the step-specific focus areas from the same template's lookup table for v7.6.9.2.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.9.2 Claude two-step prompt._
