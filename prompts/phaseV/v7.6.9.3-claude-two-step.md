# v7.6.9.3 — Struct Padding Audit (Conditional) + Phase V Closure (Claude Two-Step Prompt)

_Date: 2026-04-13 (updated — enhanced review items, aligned with agent prompt)_
_Step: V3-F_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.3.md` — session context.
  2. `prompts/phaseV/v7.6.9.3-agent-prompt-gpt-codex.md` — full spec with gate condition.

Then read every file listed in the agent prompt's §1 Required Reading section.

⚠️ CONDITIONAL STEP — measure `free_heap_internal` on C3 first.

  If ≥ 65 KB: SKIP. Produce no-change PR documenting measured heap.
  If < 65 KB: audit `SensorEntity` struct, implement if gain ≥ 300 B.

  ⚠️ MANDATORY: Check NVS serialisation safety before ANY struct change.
  Read `firmware/core/nvs-persistence.h` to determine if serialisation uses sizeof(SensorEntity) or per-field access.
  If layout-dependent: DO NOT CHANGE — document as Phase 7/v8 consideration.

---
⛔ PRE-PR GATE:
  preflight + assembly check (if fragments edited) + Playwright (if code changed)
---

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.9.3.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • If heap ≥ 65 KB: NO code changes present in diff? Only changelog and session log?
  • If heap < 65 KB: NVS serialisation safety check was performed and documented?
  • If struct changed: `sizeof(SensorEntity)` change does not break NVS restore?
  • If struct changed: all `devices[]` initialisers still compile and match the new layout?
  • Measurement result documented in issue #165 with exact value?
  • No changes to `SegmentSnapshot`, `HistoryMeta`, `PERSIST_SLOTS`, `HISTORY_HOURS`, `HISTORY_INTERVAL_MINUTES`?
  • No fragment boundary reorganisation?
  • Changelog entry accurately describes what happened (change or no-change)?
  • Session log created with measurement results, audit findings, decision rationale?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.9.3-PR<NN>-consolidated-audit-and-lessons.md`
  - Fill in `prompts/handoff/phaseV-results.md` with all step results
  - Fill in `prompts/phaseV/phaseV-conclusion-assessment.md`
  - Run `prompts/phaseV/phaseV-closure-analysis-prompt.md` in a Claude session

Tag: `git tag -a v7.6.9.3 -m "Phase V V3-F: Struct audit / Phase V closure" && git push origin v7.6.9.3`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers. Append the step-specific focus areas from the same template's lookup table for v7.6.9.3.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.9.3 Claude two-step prompt._
