# v7.6.9.1 — Satellite Hostname/IP + CSV Export Role Column (Claude Two-Step Prompt)

_Date: 2026-04-13 (updated — checkpoints aligned with agent prompt, enhanced review items)_
_Step: V3-B + V3-C_
_This document is the source from which Copilot, Perplexity, and Codex review prompts are derived._

---

## Step 1 — Agent Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor

Before making ANY changes, read the following files completely and in order:

  1. `prompts/handoff/phaseV/session-handoff-v7.6.9.1.md` — session context.
  2. `prompts/phaseV/v7.6.9.1-agent-prompt-gpt-codex.md` — full spec.
  3. §1 Required Reading, focusing on:
   - `firmware/core/web-handler.h` — `handle_aggregator_gateways_()` at line ~1431
   - `dashboard/components/gateway-panel/index.js` — functions at lines ~16, ~49, ~234
   - `dashboard/core/sensor-defs.js` — `EXPORT_SHARED_COLUMNS` at line ~12, `EXPORT_SENSOR_SUFFIXES` at line ~13
   - `dashboard/core/history.js` — CSV builders at lines ~155 and ~172

Then read every file listed in the agent prompt's §1 Required Reading section.

Then implement:

  1. V3-B: Extract hostname from satellite manifest in `handle_aggregator_gateways_()` — search within `"gateway":{` object only
  2. V3-B: Extract IP from `sat.base_url` — strip `http://` prefix
  3. V3-B: Emit `hostname` and `ip` in gateways JSON
  4. V3-B: Update gateway-panel JS: display hostname/IP, use `hostname||name` as fallback

---
⛔ CHECKPOINT A (after V3-B hostname/IP):
Run:
  grep -c "hostname" firmware/core/web-handler.h — expected: ≥ 3
  grep -c "hostname" dashboard/components/gateway-panel/index.js — expected: ≥ 3
  bash scripts/assemble-sensor-history.sh --write && --check — PASS
If ANY check fails: STOP.
---

  5. V3-C: Add `'role'` to `EXPORT_SHARED_COLUMNS` in `sensor-defs.js` (⚠️ BREAKING CHANGE)
  6. V3-C: Add `getExportRole()` function
  7. V3-C: Update `buildSingleSensorCsv()` and `buildMergedSensorCsv()` with role column
  8. V3-C: Add satellite prefix to merged export columns
  9. Version bump, full pipeline, preflight, Playwright

---
⛔ PRE-PR GATE:
  assembly check + preflight + all Playwright fixture sets green
---

Do NOT replace `EXPORT_SENSOR_SUFFIXES` with manifest-driven function (V3-D scope).

## Step 2 — Review Session

Repo — https://github.com/GCV-Sleeper-Service/ESP32-GW-multi-sensor
Read thoughtfully `prompts/handoff/phaseV/session-handoff-v7.6.9.1.md` to understand the current stage and deliveries.

After the agent creates its PR, open a second fresh session and review specifically checking:

  • Hostname extraction uses `"gateway":{` object only (not global manifest string search)?
  • IP extraction strips `http://` correctly and handles port/path in URL?
  • `handle_aggregator_gateways_()` emits both `hostname` and `ip` JSON fields per satellite?
  • Gateway selector tabs use `hostname || name` as fallback label?
  • Gateway summary and settings panels show hostname and IP?
  • `EXPORT_SHARED_COLUMNS` includes `'role'` at correct position?
  • `getExportRole()` function exists in `sensor-defs.js` and reads from `manifest.gateway.role`?
  • CSV `role` column at correct position (after `gateway_ip`, before `timestamp`)?
  • Merged export has `{sat_slug}_{sensor_id}_{metric_key}` column names?
  • `EXPORT_SENSOR_SUFFIXES` NOT replaced (that's V3-D)?
  • Backward-compatibility break documented in changelog and PR description?
  • `assemble-sensor-history.sh --check` passes (firmware fragment edited)?
  • All Playwright tests pass?
  • Session log created with ESPHome output, Playwright fixture table, evidence summary?

IMPORTANT: Once PR is merged, produce deliverables BEFORE closing the session.

Post-merge deliverables:
  - `prompts/phaseV/v7.6.9.1-PR<NN>-consolidated-audit-and-lessons.md`
  - Review and update `session-handoff-v7.6.9.2.md` and `v7.6.9.2-agent-prompt-gpt-codex.md`

Operator device testing: gateways JSON includes hostname/ip, dashboard shows them.

Tag: `git tag -a v7.6.9.1 -m "Phase V V3-B/C: Hostname/IP, CSV role column" && git push origin v7.6.9.1`

---

## External Reviewer Workflow

After the agent opens the PR and before merging, send the universal reviewer prompt (from `prompts/phaseV/pr-audit-question-template-phaseV.md`) to external reviewers. Append the step-specific focus areas from the same template's lookup table for v7.6.9.1.

The external reviewer posts findings as a PR comment. Fix any Blocking/High issues before merging.

---

_End of v7.6.9.1 Claude two-step prompt._
